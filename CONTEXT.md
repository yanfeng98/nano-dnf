# nano-dnf

一个 2D 横版动作地下城：清房间、打小 Boss 与 Boss、掉强化卡。本文件是这个项目**自己的语言**，只收这个概念（"地下城里的一次战斗"）里特有的词。放在根目录而不是 README 里，是因为 README 是切片式 devlog（记录"什么时候改成这样"），这里记录"这些词到底指什么"——当同一件事在代码、文档和业主口中有三个叫法时，以本文件为准。

空间这套词尤其要紧：地面从**一条线**变成**一块带**之后，`y` 与 `z` 的歧义会在每一处绘制、每一处判定里反复出现。

## 空间与地面

**地面带 (ground band)**：
关卡里可站立、可走位的那一横条地面。每个房间各有一条，有自己的前缘与后缘；站在带上不同的深度，就是站在地面上不同的位置。
_Avoid_: 地板, 地面区域, 地面层

**地面线 (ground line)**：
一条固定的水平线，也就是 `ARENA.groundY`。它是地面带的前缘（离镜头最近的那一行），也是深度为零的地方。
_Avoid_: 地板线, 地平线

**深度 (depth) / z**：
第二个地面轴——一个实体站在地面带上"多靠里"。与高度正交：深度决定脚踩在带上哪一行，高度决定离地多远。
_Avoid_: 纵深, Y 轴, 层级, 排, 道

**走位 (footsies)**：
在深度轴上的移动。用来绕开正面的攻击、对齐自己的技能、把怪逼到一处。
_Avoid_: 上下移动（含糊，说不清是深度还是高度）, 位移

**屏幕纵向 (screen vertical)**：
实体画在屏幕上的纵向位置，是**高度与深度的合成**：深度把它往屏幕里推（视觉上向上），高度把它抬起来。任何"这个怪怎么画在人上面/下面"的问题都在这个量上，不在 `y` 上。
_Avoid_: y, 纵坐标

**精灵锚点 (sprite anchor)**：
精灵图单元格里那个会落在角色脚底的点（`SPRITE.anchorX/anchorY`）。它保证所有动作行、两个朝向都对齐在同一个地面点上。
_Avoid_: 锚点（含糊）, 原点, 中心

**贴地锚点 (ground anchor)**：
一条效果的"归地板"而不是"归施法者"的性质。有贴地锚点的效果钉在地面带上的某一档深度：人被打飞了，它也留在原地继续烧。
_Avoid_: 地面标志, 锚点, ground 标志

**房间 (room)**：
一次战斗的单位：一组敌人（可带地形危害）与一条属于自己的地面带。房间之间不滚动，换房就是换场。
_Avoid_: 关卡, 场景, 地图

## 高度轴

**高度 (height) / y**：
一个实体离地多远。重力、跳跃、浮空、落点都作用在这个轴上。
_Avoid_: 深度, Y 轴, 纵向位置

**浮空 (launched)**：
被击飞到空中、还没落地的状态。这是高度轴上的事，与深度无关——一个怪可以在同一档深度上被挑起来。
_Avoid_: 击飞, 起飞

**倒地 (knockdown)**：
被打趴在地上、失去行动力的状态。
_Avoid_: 击倒

**硬直 (stun)**：
被打中后短暂无法行动的状态。
_Avoid_: 僵直, 僵直时间

**霸体 (super armor)**：
出招期间不被打断的状态；仍然吃伤害，但动作照旧走完。
_Avoid_: 抗打断, 霸体帧

## 判定

**判定盒 (hitbox)**：
参与碰撞的矩形。**身体盒**（`bodyBox`）贴着实体的躯体，底边就是脚；**攻击盒**（`attackBox`）由攻击的 reach 与朝向生成。
_Avoid_: 碰撞框, 包围盒, 判定区

**容差 (tolerance)**：
深度的对齐要求——两个实体在深度上差多远才算"站在同一处"。没有容差，走位就只是画面上错开，剑从怪身上劈过去却照样掉血。
_Avoid_: 判定精度, 误差

## 单位与量法

**Slayer 身高 (Slayer-height)**：
这个项目量长度用的单位，等于脚底到精灵锚点的高度（当前 156 像素）。技能范围、容差、缝隙宽度都该用它而不是像素——像素数会随精灵图重烘而变化，身高不会。
_Avoid_: 身位, 格, 单位

**大蹦**：
业主与文档对**崩山裂地斩**的昵称。它也是"贴地锚点"与深度轴的参照技能：它的地裂本来该铺在地面带上，而不是一条线上。
_Avoid_: 山崩, 崩山裂地击
