# LoopX project-local private state (registry, active goal state, run overlays).
.loopx/
.codex/goals/

# Cached DNF source files imported by assets/import_dnf_art.py
assets/dnf_src/

# Slayer skill-icon atlases (regenerate with: python3 assets/import_dnf_art.py --atlas)
assets/dnf_skillicon_atlas.png
assets/dnf_atskillicon_atlas.png

# Node dependencies and failed-run screenshots from the browser check
node_modules/
tests/browser/artifacts/playability-timeout.png
tests/browser/artifacts/playability-defeat.png

# npm lockfile resolved through a local mirror; keep the public boundary clean
package-lock.json

# Generated effect references for manual review
assets/dnf_effect_candidates.png
assets/dnf_effect_preview.png
#   (the overview sheet, the per-family filmstrips, the animated grid, the manifest)
assets/dnf_effect_berserker_*.png
assets/dnf_effect_berserker_*.gif
assets/dnf_effect_berserker_*.txt
#   (the all-packs catalog sheets, --catalog)
assets/dnf_effect_catalog_*.png
#   (layered effect animations rendered from the client art, --grid browsing)
assets/dnf_effect_anim/
#   (the body sheet cut into individual actions, make_body_action_sheets.py)
assets/dnf_body_actions.*
#   (every body variant animating, make_body_variant_grids.py)
assets/dnf_body_variants/
#   (proposed body-motion frame ranges for the 红眼 skills)
assets/dnf_src/skill-motion-candidates.png
#   (decrypted per-skill preview clips from the client)
assets/dnf_src/skill-videos/
