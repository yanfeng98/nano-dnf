# nano-dnf

通过 code agent 实现一个 DNF 风格的 2D 横版动作地下城游戏，用来检验 code agent 的端到端能力。

## 在线试玩

打开 <https://luyf-lemon-love.space/nano-dnf/>（备用 <https://yanfeng98.github.io/nano-dnf/>）
直接在浏览器里玩，无需本地安装：
清空房间后走到右侧传送门进入下一层，第 5 层击败 Boss 即通关。

该页面由 `.github/workflows/pages.yml` 在 `main` 分支更新时自动测试并部署。

## 当前切片

可玩的第一版垂直切片已经落地：一个不依赖任何第三方库的浏览器游戏，包含移动、跳跃、连击、
带前摇的敌人 AI、房间清怪与传送门推进，以及 Boss 房间通关判定。
第二切片补上了清怪回报：击杀掉落回血球，击杀累积经验并升级，升级提升攻击、生命与魔法上限。
第三切片补上了敌人威胁层次：远程术士、直线冲锋兵，以及 Boss 带预警的地面重踏（可跑出范围或跳起躲开）。
第四切片把技能改成 DNF 鬼剑士那一套：上挑 / 崩山击 / 十字斩 / 鬼斩，各带 MP 消耗、独立冷却与随等级成长的伤害，
按键也换成 DNF 布局（方向键移动、`X` 普攻、`C` 跳跃、`A`/`S`/`D`/`F` 技能），HUD 左下角新增技能栏。
第五切片重做了画面：原创鬼剑士像素精灵（待机 / 跑动 / 攻击 / 技能 / 受击 / 跳跃 / 倒地）、金币边框 HUD 与角色头像、
带冷却遮罩的技能图标栏、视差地下城背景（远近拱门、火把、雾气、暗角）、Boss 血条与打击特效。
第六切片加深了内容：Boss 房间前新增「断桥」混合房（术士远程压制 + 冲锋兵突进），
Boss 血量掉到一半后进入**狂暴第二阶段**——移动更快、出手更密、重踏范围更大，并解锁带预警的
超甲突进（`LUNGE`），HUD 的 Boss 血条会标出半血分界线并改读「GOBLIN KING · 狂暴」。
第七切片往这条关卡段里放进了**小 Boss**：断桥房的剑卫自带一个长前摇的**旋风横推**
（`SPIN`：超甲、带旋刃、命中判定比身体宽得多，而且会边转边向前推进），
贴脸硬吃会被扫中，正确解法是在前摇内退出横扫轨道或起跳越过；击倒它必定掉落更大的回血球。
第八切片让每次跑图不一样：每清空一间房就翻开 **3 张强化卡**，从中选 1 张（锐锋 +3 伤害 /
体魄 +20 生命上限并立即回复 / 灵息 +3 每秒回蓝 / 鬼气 +15% 技能伤害）。三张卡由种子决定，
同一 seed 完全可复现，不同 seed 会抽到不同组合；不选就不开传送门，所以奖励不会被顺手漏掉。
第九切片给跑图一个重玩理由：地址栏可以用 `?seed=123` 固定种子，或按 `N` 换一个新种子；
通关记录按 seed 存进 `localStorage`（最好成绩的时间与等级、通关次数），标题界面直接显示
「本种子最佳 0:52.1 · Lv 5 · 已通关 n 次」，通关时会标出「新纪录！」。
第十切片修掉一个线上才能看见的缺陷：**地牢原先在标题界面背后就已经在跑**，打开页面不按键，
回来就是 0 血 `YOU DIED`。现在只要有任何全屏界面（标题 / 帮助 / 暂停 / 通关）盖在上面，
模拟就完全停下——标题期间 `state.time` 保持 0，一滴血都不会掉。
第十一切片补上发布验证：把「暂存契约」做成离线测试（暂存脚本实际跑一遍，断言页面引用的资源一个都不少），
再加一个线上冒烟 `npm run test:live`（核对线上字节 + 无头浏览器跑真实页面），并把线上冒烟挂进
Pages workflow 的 deploy 之后，让静默落后或报错的部署直接把流程变红。
第十二切片补上配乐：`src/music.js` 是一个纯音符调度器（A 小调四小节循环、96 BPM、低音打拍 /
琶音旋律 / 噪声踩镲），`main.js` 用预读调度器把它排进音频时钟，并走一条独立音乐总线——
所以静音就是把总线增益归零，而不是让每个音符各自判断。浏览器证明现在会断言
「首次输入前不出声、之后音符在持续调度、静音把总线归零、取消静音恢复」。
第十三切片让**种子真正决定地牢**：房间池扩到 6 间（5 间战斗房 + 王座），每次跑图从中抽
4 间战斗房并打乱顺序，最后固定接「断桥」小 Boss 房与王座。抽签用独立的小随机器，
不消耗放置敌人的随机流，所以同一种子完全可复现；不同种子在顺序与**跳过哪一间**上都不同
（400 个种子里 12 种开场顺序都出现过，新增的「沉没礼拜堂」约 3/4 的种子会遇到）。
第十四切片给替补房装了机制：沉没礼拜堂的地板会**裂开再塌陷**，站在上面的谁都吃伤害——
敌人也算，所以可以把重甲兵引到裂缝上。同期发现并修掉一个真实的手感问题：种子能把全池第二硬的
「骨龛之门」排在第一间，1 级、0 强化硬撞；改成**开局房只从最温和的一半里抽**之后，
同一条触屏通路从 28 点承伤降到 0–7，四位种子的确定性成绩也从 7/7/6/0 变成**全 0**。
第十五切片把塌陷做成看得见听得见的反馈：专属的尘土/碎石效果、比地面重踏更重的屏幕震动，
以及一段低频崩塌音；`stats.collapses` 驱动音效，浏览器证明从外部盯着每块石板，
断言**每次塌陷之前都必须先观测到裂缝**（实测两条通路各 4 次塌陷、0 次无预警）。
第十六切片补上开局引导：`src/hints.js` 是纯提示选择器，按紧迫度排序、每次只显示一条、
每条一局只出现一次（陷阱优先于强化卡，强化卡优先于连击提示）。浏览器证明会断言
提示确实出现过、覆盖了强化卡这一刻、并且**会自己消失**（实测两条通路各出现 4 条、清除 4 次）。
第十七切片让链接可分享：补齐 OG/Twitter 卡片标签与页面简介，卡片图由
`assets/make_share_card.py` 用仓库自带美术程序化合成（1200×630）。发布冒烟现在会真的去抓
线上卡片图，校验 200 / PNG / 尺寸与标签一致；离线暂存检查也会断言 `og:image` 指向的文件
确实被打包——和当初漏发 `records.js` 是同一类事故，这次提前堵住。
第十八切片让标题页自己演一遍：`src/attract.js` 是一个无 DOM 的自演脚本，打开页面就能看到
鬼剑士打空第一间房、翻开强化卡、选一张、再走进传送门——它跑在**自己的 Core 状态**上，
所以标题背后那一局真实游戏依旧一动不动（`state.time` 保持 0）。
第十九切片让这段演示演到打完：整条链路（清房 → 选卡 → 小 Boss 房 → 王座）都会走完，
Boss 掉到半血进入**狂暴**时会看到 `Goblin King enraged!`，击破后画面停在
`Boss down! / LEVEL UP / Dungeon cleared!` 上 2.5 秒再从头重演——不再是一闪而过。
演示自己的横幅现在画在标题横幅之上（房间名、狂暴、通关），而浏览器证明除了开头那 6 秒，
还会把同一条演示快进到狂暴并打通，全程继续断言真实那局一个字节都没变。
第二十切片让通关画面值得一读：`src/summary.js` 是一个纯格式化模块，把这一局的
**种子 / 用时 / 等级 / 拿到的强化 / 击杀 / 承伤**列成两列表格画在 `DUNGEON CLEARED` 下面。
数字取自通关那一刻的快照（结束画面背后时钟还在走，所以不能直接读实时 state），
浏览器证明会把表格逐格与刚打完的那局对比——对不上就报错。
第二十一切片让失败画面同样交代清楚：`YOU DIED` 下面用同一张表，外加**到达层数**
（`第 3/5 层`），快照改成胜负两种结局都在结束那一帧冻结（`syncFinishedRun`）。
浏览器证明现在也会真的死一次（走 `Core.damagePlayer` 的正规伤害路径），逐格核对失败画面，
并单独校验表里的用时就是死亡时刻——两处对不上都会失败。
第二十二切片让一局可以传给别人：结束画面给出这一局自己的链接
（`…?seed=20260915`，已有的 `touch` 参数与 `#hash` 都保留），页面下方多一个「复制本局链接」按钮；
浏览器证明会**照链接重开一页**，断言种子与房间顺序完全一致，再点按钮把剪贴板读回来核对。
第二十三切片让暂停画面也交代这一局：`PAUSED` 下面复用同一张战绩表，但读的是**实时**数字
（`Summary.liveRun()` 直接映射 Core state），所以暂停时看到的就是屏幕上那一局；
浏览器证明会在跑图中真的按一次 P，逐格核对表与实时状态，再确认已经继续跑起来。
第二十四切片给跑图一条配速线：HUD 下方显示这个种子的最佳时间，并标出当前是领先还是落后（`Summary.pace()`，纯函数）；浏览器证明会断言首局显示「本种子还没有记录」、有记录的新一局显示领先，并在时钟越过记录时**恰好翻面**成落后 12 秒。
第二十五切片让页面记住**历史最佳**：`Records.bestOverall()`（纯函数）跨所有存过的种子找最快通关，
并列时归较小的种子（不受键顺序影响），读不出来的条目直接跳过；标题右上角在本种子记录下面多一行
`历史最佳 0:52.0 · 种子 20260915`。浏览器证明会断言首局还没有这一行、通关后这一行与存储记录逐字一致。
第二十六切片修掉发布门自身的一个**不可信断言**：浏览器证明原先要求整局承伤 `<= 24`，
而机器一忙，策略反应变慢、承伤自然上升（同一天出现过 29，重跑 7），于是门会误报。
现在改成检查**单次受击**不得超过游戏自己给出的最大一击（由 `ENEMY_TYPES` 与狂暴倍率算出，实测 22），
整局承伤只作为观察值写进报告；证明末尾还有一条**自检**：喂给它一次 60 点的假受击必须被抓住、
喂一次 13 点的正常受击必须放过——只在两种情况下都正确时才认为门是好的。
第二十七切片让触屏也能暂停：触屏布局原先只有 ← → 跳 攻 音 编，没有暂停键，所以用手指玩
根本到不了那张实时战绩表。现在右上角多了一个「停」（`Render.TOUCH_LAYOUT` → `hitTestTouch`
→ 与 `P` 键同一个开关），按下后它翻成「续」并**画在遮罩之上**——遮罩会把唯一的出口盖住；
浏览器证明的触屏那条通路改成真的点这个键进暂停、再点一次继续（键盘那条仍按 P），
所以这个键一旦掉了，证明会直接红。
第二十八切片把**血之狂暴**做成它本来的样子：这是一个 **Buffer**，不是有时限的增益——
按业主的说明，开一次就一直在，**再按一次才取消**（`SKILLS.frenzy.buff.duration = Infinity`
+ `toggle`，第二次施放不扣血），吃到它以后全身变红、普攻变双刀，**打怪有几率出「血球」**，
血球自己飞进红眼身体里回血（`BLOOD_ORB`：34% 几率、+4 HP、1.6s 内归位，掉在地上也不会被重力
拽走）。角色动作取身体动作 **第 22 段**（帧 161-169，站起来张手那一下）；图标取官方图集
**134 / 135 这对**（134 是技能图标、135 是它的灰版，开着的状态就按 135 画在头顶上）；
普攻的双刀刀光和血球都出自 `effects.png` 第 5 行——那一行原本把双刀血气和血球堆在一起，
现在拆开了：技能自己一行放双刀，血球单独一行（`EFFECT.orbRow`），落下来的血才不会带着刀光。
第二十九切片修掉三个动作问题（业主逐条看过）：

1. **普通攻击的「两次同方向向后甩刀」是个真 bug**：四段原本按等宽的 10 帧去切，切点落在
   客户端自己的动作中间——第一下收在第二下的起手里、第二下又从同一个起手甩一次，中间还夹着
   客户端的站立空动作。现在四段就是客户端的四个动作（1-8 / 8-15 / 18-26 / 29-39），
   两段之间的站立（16-17、27-28）永远不播，测试会走遍每一段的每一帧确认它不越界、不踩站立段。
2. **崩山击的多余动作**：起手原先带着动作 17 的三帧「站起来」，所以蹲下之后又站起来才砸。
   现在起手 = 蹲下（128）+ 动作 26 的举剑（203-205），落点仍是 206-208，节奏还是 4 帧起手 /
   3 帧砸地。
3. **崩山裂地斩不再和崩山击一样**：它原先没有自己的动作，只能借通用技能行的那记新月劈。
   现在用自己的动作 29（227-237：举剑过头 → 大回扫 → 低位收招），并且**放大范围**：
   剑锋 120 → **200**、冲击波 240 → **360**、高度 22 → **36**，一记裂地能扫到三分之一场地。
第三十切片接着修上一条没治好的两处（业主复看后指出「还是有」）：

1. **普通攻击那一「甩两下」的真正来源**：身体图是**从半途开始的**——第 0-4 帧是上一轮最后一击
   的**余尾**（刀光的峰值在帧 2，帧 3-4 是正在衰减的弧），而这一轮的四击峰值在帧 **13 / 24 / 36 /
   45**。我们却把攻击行从帧 0 烘起，于是第一下前面多挂了一道别人的刀光。现在攻击行从**帧 8**
   烘（`cells.attack = 8-49`），四段 = 帧 8-15 / 18-26 / 29-39 / 39-48，测试直接读回烘焙好的图，
   断言整行只有**四个刀光簇**、且必须落在列 **5 / 16 / 28 / 37**（= 客户端的 13/24/36/45），
   从 0 烘就会整体错位并多出那一道余尾。
2. **崩山击/大蹦在空中的「自动攻击」是渲染分支顺序的 bug**：`playerFrame` 先判断「不在空中」，
   于是技能跳到半空时，画面被切到通用的**跳跃/下落帧**（`extras` 行），落回地面才又切回技能动作——
  看起来就像空中多打了一下。现在**带 clip 的技能在空中继续播自己的动作**（`skillClip` 优先于空中帧），
   只有没有 clip 的技能才退回跳跃帧；单元测试与浏览器证明都盯着这一条。
第三十一切片按业主第三轮反馈再收一遍（这次先把「数据」摆出来再下手）：

1. **普通攻击四段确实是「重复的」**：客户端身体图里第二击帧 13 与第三击帧 24 **像素级相同**
   （差 3 个像素），第四击的帧 36/45/54 也是同一张，所以看着「后退两下、向上两下」不是错觉。
   现在四段是四个**不同方向**的挥砍：**前下劈 → 后扫 → 上挑过头 → 头顶下劈**，每段自带起手与收招、
   刀光都落在该段的第 4 帧；测试把四个刀光帧两两比对，要求差异足够大（旧的四段里有像素级相同的对，
   会直接红），另外还要求整行没有「上一击的余尾」。
2. **崩山击**：动作只剩**举剑（203-205）+ 砸地（206-208）**，去掉了 128-131 那段「蹲下又起身」的
   第二套预备动作；跳跃由 -520 提到 **-780**（顶点 57px → 约 **138px**，滞空 ~0.71s），砸地与落地
   对齐（1.05s）。测试会把整条弧线跑一遍，要求**顶点 ≥120px**、且命中发生在落地之后。
3. **崩山裂地斩**：身体动作换成**动作 27 的旋身（211-217）**——一个连续的横扫回旋，不再是举剑砸地，
   所以跟崩山击不再撞脸；跳跃 -640；伤害改成**落地后**的两段（0.72s / 0.95s）；主判定也从
   「正前方盒子」改成**以自身为中心半径 190**：前跳会越过目标，原来落地时脚下的人反而打不到。
第三十二切片按业主第四轮反馈收口：

1. **普通攻击砍成三段**：业主确认四个动作对了，但要去掉最后一个「把剑往下」的（头顶下劈）。
   攻击行因此收在 23 帧（前下劈 0-6 / 后扫 7-15 / 上挑过头 16-22），`maxCombo` 从 4 变 **3**，
   每段的刀光仍落在该段第 4 帧。
2. **崩山击 = 先跳、再拍**：空中播客户端**自己的起跳姿势**（帧 232，和普通跳跃用的是同一张图），
   落地才播**砸地**（206-208）。之前那三帧「举剑过头」（203-205）就是业主说的多余动作。
3. **崩山裂地斩也是这个形状、大一号**：同样起跳（232）+ 更重的跺地（229-231），区别交给它自己的
   巨剑与裂地特效（以及半径 190 的主判定、360 的裂地波）。
第三十三切片收尾两处：

1. **崩山击的「跳」要有跳跃的样子**：之前整个滞空只有一个姿势撑着，看着不对。现在把客户端自己的
   跳跃动画整段烘进去——站立（176，起跳前）→ **起跳（232）→ 下落（236）** → 落地砸剑（206-208），
   节奏 1+2+3 帧，起跳/下落正好铺满 0.71s 的滞空。
2. **大蹦是整块特效变大**：业主的原话是"整个特效变大，不是光冲击波"。`EFFECT.draw.mountainRift`
   从 250px 放大到 **430px** 并整体上移（`dy -110`），巨剑与砸地血光几乎铺满半个画面高度，
   冲击波（360）保持不变。
第三十四切片：业主给了**确切帧号**，先把两套皮肤的号对齐再改。

- **帧号对齐**：业主读的是 `sm_body0048`（红眼觉醒皮肤，210 帧）的全帧图，我们烘的是
  `sm_body0000`。逐帧比对确认 **`sm_body0000 = sm_body0048 + 1`**（0048:42 ↔ 0000:43，
  0048:45 的大上撩弧 ↔ 0000:46，逐帧一致），之后业主给的号都按这个换算。
- **崩山击的「跳」**：客户端里跳跃有两种形态——只按 `C` 是 0048:126-131，按 `C` 再按 `X` 是
  133-136，按 `C` 再按 `Z` 是**银光落刃**（跳起下刺、落地冲击波）。崩山击用的是**跳跃**那一套，
  所以烘成 0000:127-132；之前我用的 232/236 是普通跳跃的**另一套**动作，这就是"跳起来不对"。
  落地依旧是砸地 206-208，节奏 6+3。
- **`Z` = 上挑**：业主给的 上挑 = 0048:42-50 → 这里 **0000:43-51**（旧的是 41-50，早了两帧、
  还漏了 51 帧的收招），现在按客户端的上挑动作从头播到尾。`Z` 的绑定本来就是 `upSlash`
  （实测按住 `Z` → `cast=upSlash`，动画落在技能行第 6-14 列）。
第三十五切片把空中那两招做出来，并修掉"上挑离开技能栏就失灵"：

1. **`Z` 不再受技能栏影响**：`currentInput()` 原先把"技能自己的键"限制在**已经装在技能栏里**的技能上，
   于是把上挑挪出栏位（或从旧存档里读回来时缺了它）就按不出上挑。现在按 `Loadout.SKILL_KEYS`
   遍历——上挑永远在 `Z` 上。浏览器证明会先把上挑从技能栏里拿掉，再按 `Z` 确认照样能放，
   最后把技能栏放回去。
2. **空中的 `X` = 跳跃攻击**：客户端 0048:133-136 → 这里 **134-137**（4 帧），烘在攻击行尾部
   （第 23-26 列）；离地状态下按 `X` 播这套，而不再只是挂着跳跃姿势。
3. **空中的 `Z` = 银光落刃**（新招式）：跳起来后按 `Z` 变成俯冲下刺——`dive 980` 直接把他压向地面，
   落地命中 + 落地冲击环（reach 150）+ **60% 几率**把敌人打倒（判定走状态 RNG，种子照样可复现）；
   动作取 134-141（跳跃攻击 + 落地那一段，**这组帧是我挑的**，业主没给参考），特效把上挑的弧线
   **转 90°** 画在身前（`EFFECT.diveRow`）。它不进技能栏、没有图标：`CASTABLE_SKILLS` 只在冷却表里
   多带这一个 id。
第三十六切片修跳跃本身与俯冲落点：

1. **普通跳跃（只按 `C`）改成客户端自己的跳跃动画**（0048:126-131 → 这里 127-132）：之前用的是
   232/236 那两张，而它们**带着空中挥剑的白色弧线**——所以按一下跳跃都像在挥剑。现在把 6 帧烘进
   `SPRITE.jump`，按"已经下落多少"逐帧播，起跳 → 滞空 → 落地一路走完；只有**空中施法**（没有自己
   动作的技能）才退回通用空中姿势。
2. **银光落刃落点往前**：俯冲带 `diveForward 300`，并且俯冲期间不再被"施法中减速"把前进速度吃掉
   （和跳跃的 `leaping` 一样豁免，`divingDown`），实测落点在起跳点前方 20px 以上（有断言盯着）。
第三十七切片按业主的截图改界面与手感：

1. **银光落刃落地即停**：俯冲在空中带前进速度，但**触地那一帧就把 `vx` 归零**——业主说"到地上
   就不要往前冲了"，同时"施法中减速豁免"也只对空中的俯冲生效。
2. **左上角头像只截头部**：原来画的是整格待机像素（连身体一起），现在只取头部那一小块放大填进框。
3. **技能栏不再盖住地面**：地面线从 `groundY 460` 提到 **430**，技能栏仍在底部（`height - 96`），
   所以脚底下的地面完整露出来，角色站在栏上方。
4. **两行按键顺序改成键盘的顺序**：**Q W E R T Y 在上、A S D F G H 在下**（之前是反的）。
   槽位编号与按键没变，只翻绘制行；命中测试用的是同一张表，所以拖拽跟着画面走。
5. **技能可以从技能栏卸下来**：从槽位往外拖（放到空白处，或拖回它自己的技能卡）就清空该格并存档。
  技能比槽位少一格，业主点名"上挑卸不掉"，现在可以了；浏览器证明会真的拖出去并核对存档。
第三十八切片按业主反馈重做**怒气爆发**与**崩山裂地斩**的特效（业主：「技能特效不对」）：

1. **两个技能原来是把整个特效包叠起来**，而 DNF 的每个包都**按调色板存了好几份同一套图形**：
   没有前缀的是素色版，`(tn)` 与 `(18)` 是同一套图形换了色板。通配符把三份全都拉进来了，
   崩山裂地斩因此烘了 **29 层**（其实只有 9 个形状），等于每个血环、血柱都画了两三遍，
   还是红 + 橙 + 橙混在一起。现在一次只取一个色板，`*` 也只展开选定色板，并且**按客户端自己的
   预览视频取色板**：怒气爆发是 **`(tn)` 的白金火柱**（实测素色 `blood-front` 是 `[204,28,0]`、
   `(tn)` 是 `[249,236,200]`），大蹦是 **`(tn)` 的橙火 + 岩浆纹**；只有怒气爆发脚下那摊血
   （`blood_floor.img`，包里只有素色版）保持红色。上一轮按"红色怒气圈"的文字描述保留红板是错的：
   文字说的是形状，颜色以客户端视频为准。
2. **多层的取样时间轴原来是对不上的**：短图层会被取模循环，一个 6 帧的血环在 13 帧的合成里
   重播两三次，合成帧是**客户端根本不存在的一瞬间**。现在每个图层按同一条时间轴取样。
3. **特效包不是居中就能对上人的**：包里的图层共用一套客户端坐标，人站在包自己的血环/裂缝
   中心，而不是包围盒中心。烘图时按 `PICKS` 里声明的**地面锚点**摆位（锚点落在格子底部
   75% 的那条地面线上），渲染时把这条线对到角色脚下——之前怒气爆发的血柱飘在他左上方，
   就是因为按包围盒居中了。
4. **崩山裂地斩的巨剑**在导出里只有 20×44（客户端靠技能动画数据放大，导出不带这个数据），
   所以按落地中心放大 5 倍烘进那一行，否则"血气巨剑砸地"看起来只剩一个圈。
   顺带去掉只会在叠加混合下才正确的黑色血痕图层，以及 `*dodge` 这类同形状的重复条目。
5. **裂缝要抬到看得见**：裂缝是个 222px 高的椭圆，而 `groundY 430` 到技能栏（444）之间只有
   14px 地面，按几何把锚点摆在脚底等于整条裂缝都藏在栏后面，所以大蹦整块再抬 60px，
   与已定稿的崩山击（`dy -30` / 236px）同样的处理。
6. **业主第二次复看后的两处**（「怒气爆发好像两个技能」「大蹦火太少」）：
   - **合成帧的坐标系是错的**：以前每一帧都按"这一刻可见的内容"单独裁剪，画布原点每帧都变，
     而锚点是客户端坐标——两者不是一个坐标系，等于没对齐。实测锚点被算到画布宽度 97% 处，
     整块特效甩到角色**左侧约 120px**（特效重心 x≈375 / 角色 x≈500）。现在所有帧共用一张覆盖
     整组的画布（也顺带治好了图层进出时的抖动），特效重心与角色差 **< 32px**。
   - **怒气爆发只取"围着人"的那一簇图层**：包里 `b-01`、`blood-b`、`blood-front`、
     `blastbloodhit` 的内容重心在锚点左边 184~276px（是打点位置的层），叠进去就像两个技能同时播，
     现在只留 `blood_floor_front/back` + `blood-back` + `bloodred` + `blood` + `blood-d1`。
   - **大蹦的火放大**：`bloodsexp_1`(108×127)、`bloodsexp_2`(88×179) 在 445×166 的裂缝画布上只有
     两道火舌，客户端靠动画数据放大，现在 ×2.5（`glow` ×1.6，巨剑 ×5），火焰才成规模。
第三十九切片换掉大蹦的素材（业主：「大蹦的素材不对」）：

1. **崩山裂地斩改用客户端自己的「火」对料**：前几轮一直在 `outragebreak` 包里（那套画的是暗红
   血气，`(tn)` 板才发橙），业主判定素材不对。客户端基础包 `sprite_character_swordman_effect.NPK`
   里就有一对火：**`fire-front.img`（火焰巨剑劈下的月牙）+ `fire-back.img`（地面岩浆地火）**，
   也正是官方预览里大蹦的画面、以及最初选型表给这一招的条目（`effect/fire-front.img`）。现在这一行
   只由这两条组成（7 帧，`dx 40 / dy -90 / size 360`），`outragebreak_*` 全部不再使用；测试新增
   断言：这一行必须引用 `fire-front`/`fire-back` 且不得再出现 `outragebreak_*`。
   跨包叠图层是不行的——每个包的图层坐标属于各自画布，混着叠火焰会跑到左上角去。
2. **怒气爆发维持上一轮的结论**：`(tn)` 白金板、只取围着施法者的那一簇、锚点落在脚下，
   实机是"金环 + 白金火柱从里面喷出来"的**一个**特效（对照图见 `assets/dnf_effect_anim/`）。

测试盯着这两条不变量：`PICKS` 里同一个形状不许出现在两个色板，两个爆发技能必须声明锚点，
且怒气爆发 / 大蹦 / 崩山击三行**互为不同的图形**（业主之前抱怨过"大蹦跟崩山击一样"），
怒气爆发的墨色必须是**白金**（`green > 150 && blue > 100`；素色板那版是 `[141,9,2]`，会红）。
第四十切片把大蹦的选型重新交回业主（业主：「目前崩山裂地斩技能特效不对，请你找所有鬼剑士跟
崩山裂地斩相似的技能，注意技能要包含所有帧，我自己审核」）：

1. **先把"所有鬼剑士"钉成一份可核对的清单**：客户端 `Video/Swordman/` 里就是 84 个
   鬼剑士技能预览（`assets/dnf_src/skill-videos/`，第十九切片解出来的）。我给每个片段算了
   一个**地面喷发分**（画面下半部的血气/火焰像素数，取最旺的连续 5 帧平均），
   84 个全列进 `assets/dnf_effect_anim/rift-candidates.txt` 末尾，所以哪一条没进候选都能查。
2. **候选按"砸地 + 裂地/喷发"这一型收的，一共 15 条**（`rift-candidates.png` 是编号菜单）：
   同型的 `HopSmash`（崩山击，已在用）/ `OutRageBreak` / `ATMountainCrash` / `ATEarthPressure` /
   `ChargeCrash` / `ChargeCrashEx` / `MeteorSword` / `Hellbenter` / `FireWave` /
   `ATFistOfHellfire` / `Flame` / `NormalWave`，加三条**对照**：`BloodBlast`（怒气爆发）、
   `BloodBoom`（浴血之怒）、`TombStoneRain`（墓碑雨）。分数高但形状不对的十条
   （魔煞血陨、波动剑、卡赞红圈……）也逐条写了不入选的原因。
3. **每条候选都给到"所有帧"**：`assets/dnf_effect_anim/rift-<slug>.png` 一张图里是
   **该技能自己的预览条 + 它那个特效包每一条图层的全部帧**。色板按行分开
   （素色 / `(tn)` / `(18)`），只有跟上一层像素完全相同的板才折起来收起——
   上一轮"暗红还是橙火"的判断就栽在色板上，这次让它一眼可见。
4. **最像的是 `OutRageBreak`**（`rift-outrage-break.png`）：客户端自己的预览就是
   跳起蓄力 → 地面喷出橙火岩浆；包里同时有 `outragebreak_bloodsword_none.img`（血气巨剑压下，
   20 帧）、`outragebreak_floor.img`（地面裂开，11 帧）、`bloodsexp_1/2`（火舌）与
   `drops_*`（火星），并且 `(tn)`/`(18)` 板正是预览里的橙黄，素色板是暗红。
   结论等业主点头，**这一片只出候选，不动游戏里的那一行**。

工具：`assets/make_slayer_rift_candidates.py`（`--check` 会核对 15 张细图、色板折叠与
"清单覆盖全部 84 个预览"这几条）。
第四十一切片照业主的点名把大蹦做出来（业主看过 `rift-outrage-break.png` 后：「这张是对，
实现呀」）：

1. **大蹦改成 `outragebreak` 包本身**，一层一层叠：`outragebreak_bloodsword_none.img`
   （召唤的血气巨剑压下，20 帧）+ `outragebreak_floor.img`（地面裂开，11 帧）+
   `outragebreak_bloodsexp_1/2_none.img`（裂口里喷出来的火舌）+ `bloodsexp_glow`
   （火后面的光）+ `drops_1/2`（火星）+ `part`（碎岩）。色板用 **`(tn)`**（客户端预览的橙黄；
   同一套形的素色板是暗红），所以 `EFFECTS` 的 `fire-front/fire-back` 那一对不再使用。
2. **每一层带自己的缩放和落点**：客户端是靠技能动画数据放大这些层的，导出不带这个数据，
   所以烘焙器新增了"层里写 scale / 自己的色板 / (dx,dy) 偏移"三个可选字段。巨剑 ×1.4、
   火舌 ×2.5、光 ×1.6、火星 ×2.0、碎岩 ×1.5。两处偏移是有原因的：**巨剑往前推 240px**
   （本作技能特效画在角色**之后**，按原坐标它的剑尖正好落在脚上，整把剑藏在人背后，收招那
   一甩还会从人身上扫回来），**碎岩挪到落点**（`part` 是粒子层，包里的坐标在原点，游戏自己
   撒，导出只剩堆在一起）。
3. **锚点 = 那道裂缝的中心**（`outragebreak_floor.img`，444×166 在 x=160,y=198 →
   `(382, 281)`）：烘焙时这条线落在格子的地平线上，画的时候 `dy = -size/4`，裂缝正好在
   他脚下张开，而不是挂在膝盖上。
4. **起手对齐落地**：这一行 20 帧，原来按"活动窗口"会在落地前 0.2s 就开播（人还在空中地上
   已经喷火）。`EFFECT.timing.mountainRift = {from 0.655, to 0.98}` 让这套图**触地那一下**
   才开始：巨剑落 → 地裂 → 橙火喷出来。整块特效的 `size` 360 → **480**（业主"整个特效变大"），
   `dx` 40 → 20，`dy` -90 → -120。

实机逐帧核对（`tests/browser/capture-effect.mjs` 把页面时钟放慢 8%，1.05s 的施法可以一帧一帧截）：
0.70 落地起裂、0.75-0.89 橙火柱长到半屏、0.97 收尾只剩火舌与裂缝，与客户端
`OutRageBreak.avi` 的"地面喷橙火岩浆"对得上。测试那条不变量也跟着反过来了：这一行必须引用
`outragebreak_bloodsword_none/floor/bloodsexp_1_none`、必须声明 `(tn)` 与锚点，
且**不得**再出现 `fire-front/fire-back`。
第四十二切片按业主的判断把这一行**拆成阶段**（业主：「这个技能应该是多个技能特效组合的」）：

1. **解剖**：`assets/make_rift_layer_plan.py` 把 `_outragebreak` 包摊开 —— 29 个条目其实是
   8 种图 × 3 条色板（`plain` 血红 / `(tn)` 橙火 / `(18)` 与 `(tn)` 逐像素相同）再减去 4 条
   没重发的，247 帧。业主看到的"好几个技能"就是这包里的四组：**血剑**（火刃斜劈下来，落地
   炸开）、**地裂**（岩土碎裂 → 黑裂纹 → 熔岩环由小到大 → 碎石飞起 → 裂纹贴地蔓延）、
   **喷发**（`bloodsexp_1` 矮宽火丛 / `bloodsexp_2` 细高岩浆柱两条不同形状）、**点缀**
   （`glow` 补光、`drops_1/2` 岩浆滴、`part` 碎岩）。色板只取 `(tn)`；`_ldodge` 是同一形状
   压暗的孪生图（总不透明度只有亮版的 1/4），出口不用。输出：
   `assets/dnf_effect_anim/rift-outrage-break-layers.png` 与同名 `.txt`。
2. **烘焙器加 `stages` 模式**：每条图层写自己的 `from`/`until` 窗口（可带 `frames` 取这条图的
   哪几帧），在窗口内铺自己的帧、窗口外不画。同一条图可以写两次：大蹦的环在空档期**留在地上**
   （`floor` 的 f5 撑满 0.30-1.00），裂纹从环里爬出来，第二波岩浆再把 `bloodsexp_1/2` 播一遍。
3. **前后两层**：客户端把一个形状的暗份画在人后面、亮份画在人前面，这一包自己就带着这两份。
   所以技能行只留**地裂 + 血剑**（人身后），火焰、补光、碎岩进 `FRONT_ROWS`，在 `drawPlayer`
   之后画（`EFFECT.frontRows.mountainRift`）。两行**共用同一个窗口和同一个锚点**（烘焙器里
   `match` 字段把窗口从技能行传过去），否则火就不从裂口里出来了。
4. **一次长的施法**：客户端那段预览是 100 帧 @30fps，落点在第 23 帧、一直喷到第 93 帧。所以
   `duration` 1.05 → **2.0s**、`activeFrom` 仍是 0.72s（`leapFrom` 0.12 → 0.07，跳跃本身
   没变，只是提前起跳以落在同一个 0.72s）、`activeTo` 0.95 → 1.95，`hits` 2 → **3**：
   剑落一下、地裂研磨一下、第二波岩浆一下。**冲击波跟着第一下**（新增 `shockwaveHit`，
   默认仍是最后一下），因为它属于砸地的那把剑，不属于岩浆的最后一次跳动。
   这一行 45 帧，`EFFECT.timing.mountainRift = {from: 0.26, to: 0.99}`。

实机逐帧核对（`node tests/browser/capture-effect.mjs mountainRift 30 0.12 500`）：起跳阶段地上
什么都没有、0.30 血剑扫过并砸开地面、0.30-0.61 第一波橙火、0.70 只剩地上的熔岩环与裂纹、
0.78 第二波岩浆柱冲起来、1.0 散成火星而环还亮着 —— 与客户端预览的"落地 → 第一波 → 空档
只剩环 → 第二波"一致。测试跟着加了 `大蹦 plays in stages, not all at once`（窗口顺序、
同一形状不许同时画两遍、第二波窗口里必须落着第三下、剑的窗口必须收在 `activeFrom` 上）。
第四十三切片修业主看片后的两处（业主：「好像地裂圈有点小，然后什么好像需要再次优化」）：

1. **地裂圈对上命中范围**：包里最大的那道环是 239px 客户端画布，出口成 **232px**，而这一招
   自己的半径是 190（直径 380），客户端的裂缝又占它屏幕约 60% —— 所以看着小。现在整块地面
   （碎地 / 熔岩环 / 贴地裂纹）放大 **1.8x**，`size` 480 → **840**（`dy = -size/4` = -210，
   锚点才还在脚底），环落在 ~400px、裂纹 ~520px、砸开的地面 ~745px。**剑和火保持原样**：
   放大地面会把窗口撑大，`size` 跟着窗口一起放大，被真正放大的只有地面那几层。
2. **碎岩其实一块都没画**：前排行里 `part` 我漏了 `board`，`palette_name` 去找
   `(tn)outragebreak_part.img`（这一条压根没有 `(tn)` 板），整层被静默跳过。两条 `part`
   现在都写 `"board": ""`（素色板），弹幕里能看到翻滚的碎石。
3. **第一波的火把角色埋了**：火按客户端的层次画在人**前面**，居中时整个人被盖住、只剩剑尖。
   客户端里人是站在喷发左边，所以第一波的火右移 150px —— 它仍然从裂圈里冒出来（环 400px 宽），
   人也能看清。
4. **火更宽**：两波火 2.5 → **3.2**、岩浆滴 2.0 → 2.6，第二波把 `bloodsexp_1` 右移 120、
   `bloodsexp_2` 左移 60，让"细高柱子"读成一堵岩浆墙（客户端第二波就是这么宽）。

顺带修了测试自己的一个漏洞：窗口解析那条正则原来不跨行，`stages` 里一个跨两行的条目会被
静默漏掉（这一片就漏了 4 条），现在它会比对 `"entry":` 的出现次数，漏读就直接失败。
第四十四切片按业主点名的四条继续收（业主：「空档期那片裂纹偏亮…冲击波弧比熔岩环大…
血剑落点偏左…并且感觉火焰柱子少一块」）：

1. **每层 `alpha`**：烘焙器的 stage 多了 `alpha` 字段（`dim()` 按比例压 alpha），空档期那片
   贴地裂纹改成 **0.5** 强度；并且把**熔岩环挪到裂纹之后画**（更晚入表 = 画在上面），
   于是暗地面上只剩环在亮，和客户端预览 f32-59 对得上。
2. **冲击波弧只给大蹦收**：`shockwave.visual`（默认 1，大蹦 0.7）只缩**画出来的**那圈弧
   （660 → 约 460），伤害判定框一点没动。
3. **血剑落点往右**：偏移 240 → **320**，剑不再落在裂缝左半边，而是砸在火起来的那一块上。
4. **"火焰柱子少一块"是火的开膛**：包的 `bloodsexp_1` 火丛中间本来就是空的（客户端里**人站在
   那个空里**），我上一片把整波火推到旁边，等于把这个空当成了缺口。现在第一/第二波的火都回
   到 `offset -30`（空膛套在人身上），第二波的柱子改 `offset -85` 正好**填进那个空膛**，
   柱子不再像被咬掉一口。

验收：`npm test` 153/153、`npm run test:browser` 双通，慢放逐帧：触地时地面裂开且裂纹偏暗、
第一波火在身侧、空档期**只有环亮着**、第二波整根柱子从环里拔起、收尾散成火星。

第四十五切片把大蹦的火改成一排矮柱子（业主看着包里那张图说：「好像崩山裂地斩火焰太高了，
火焰柱子应该是多个火焰柱子」）：

1. **先量一下有多高**：上一片为压住「柱子少一块」把两波火都放大到 ×3.2，出口成屏幕上第一波
   火丛 **378px**、第二波岩浆柱 **533px** —— 角色才 ~120px，第二波比 540px 高的竞技场还高。
   第一版改法（四株向天的岩浆柱，200-270px）交回业主后仍然偏高，所以**两波都压到角色高度
   附近**：第一波 124-142px、第二波 155-180px（约 1-1.5 个角色高）。
2. **第一波用包里的「矮宽火丛」**：这一波不是柱子，而是 `bloodsexp_1`（那张图上中间带空膛、
   四周一圈火舌的那张），**摆三处**（距环心 −170 / 0 / +170）——火舌本来就是多个，所以这一波
   读成"一排小火"，而不是一根高柱；中间那株最先起、也最高（×1.15，套在人身上当火根）。
3. **第二波才是柱子**：客户端第二波换成 `bloodsexp_2`（细高岩浆柱），摆四处（距环心
   −180 / −60 / +60 / +180），中间两株先起（×1.05），外侧两株晚一点、矮一点（×0.90）。
   四株是**围着人**摆的：这一行画在人**前面**，柱子立在人站的地方就会把人整个盖住（上一片
   那丛宽火就是），中间空出的空档正好站人，和客户端预览里「人站在喷发中间」一致。
4. **补光、岩浆滴、碎岩一起收小**：补光 ×2.0/×2.6 → ×1.15/×1.1，岩浆滴 ×2.6 → ×1.4，
   碎岩 ×1.5 → ×1.1（落点仍在裂口），收尾用火丛的 `frames (5,6)` 火星（×1.15）。

数值一览（`mountainRiftFire` 行 45 帧、14 段窗口）：补光 ×1.15 · 火丛 ×1.15/×1.05/×1.05
（第一波三处）· 岩浆柱 ×1.05/×1.05/×0.90/×0.90（第二波四处）· 岩浆滴 ×1.4 · 碎岩 ×1.1 ·
收尾火星 ×1.15。测试 `大蹦 plays in stages, not all at once` 跟着改了两条：「同一形状不许同时
画两遍」的判定现在**认偏移**（同一个形状站在不同地方是柱子，站在同一个地方才是重复叠图那个
bug），原来那条「岩浆柱只有两次」改成「第一波至少三处、第二波至少三处、各站各的地方，且这一
行里每一处火都在 ×0.6-1.2」。

验收：`npm test` 153/153、`npm run test:browser` 双通，慢放逐帧：落地时三处小火从裂口里起来、
空档期只剩环亮着、第二波四处矮柱子起来、收尾散成火星。

第四十六切片再压一次火，并把「圈」和火合成一件事（业主：「还是偏高，应该是多个低矮的火焰，
另外圈和火焰没合在一起」）：

1. **火再矮一档**：两波都降到膝盖高度 —— 第一波火丛 ×0.70/×0.60（86px、74px）、第二波岩浆柱
   ×0.60/×0.55/×0.50（104px、96px、87px），都不到角色的一个身高（第四十六切片之前是
   124-180px）。每一波**五处**：距环心 0 / ±110 / ±210 客户端 px，正好铺在裂缝那道环的宽度里
   （包的 `floor` 环 239×112 放大 1.8 倍，两侧各到约 190px）。
2. **火根落在环上**：包里的环是躺在地上的一片椭圆，所以**站在椭圆远侧边缘上的火**才读成"从
   环里长出来"。火丛自己的图本来就挂在锚点下方约 37px，所以它写 `offset y = -55`、岩浆柱
   （本来就以锚点为根）写 `offset y = -20`，两者的火根都落在同一条带上——离地平线约 20px、
   正是光环那圈亮带的位置；火光不再被地平线上那条被 HUD 裁掉的部分盖住。
3. **圈和火同心**：引擎自己那道冲击波弧原来画在**身前 `reach * 0.4`（144px）**处（崩山击的
   波是往前推的，所以通用写法如此），而这一招的火是围着他喷的 —— 于是屏幕上是一圈火**旁边**
   躺着一个圈。`shockwave.arcOffset`（默认 0.4，大蹦写 **0**）把弧钉在人身上：现在那道环跟
   火光同圆心，落地那一下看起来就是"地面裂开一圈、火从这圈里冒出来"。
4. **补光收小**：×1.15 → ×0.9（火变矮之后，一大片柔光不是"闪一下"，而是把火洗白），岩浆滴
   ×1.4 → ×1.3、碎岩 ×1.1 → ×1.0。

数值一览（`mountainRiftFire` 行 45 帧、17 段窗口）：补光 ×0.9 · 火丛
×0.70/×0.70/×0.70/×0.60/×0.60（第一波五处，`y -55`）· 岩浆柱
×0.60/×0.60/×0.55/×0.50/×0.50（第二波五处，`y -20`）· 岩浆滴 ×1.3 · 碎岩 ×1.0 · 收尾火星
×0.70；`mountainRift.shockwave = { reach: 360, visual: 0.7, arcOffset: 0, … }`。测试跟着钉了
两条：这一行的每一处火都在 ×0.4-1（"低矮"），以及 `arcOffset === 0`（弧画在火上，不是旁边）。

验收：`npm test` 153/153、`npm run test:browser` 双通，慢放逐帧：落地时一圈低火沿裂缝起来、
冲击波弧与火同心、空档期只剩环亮着、第二波同一批位置再来一次、收尾散成火星。

第四十七切片给大蹦补上起手的**举剑**，并把火从"一排矮火"改成"一排柱子"（业主：
「崩山裂地斩是先举剑，参考 123-124」、「它是多个火焰柱子，喷发」——他说这两句时看的是
线上那版，线上还是更早的**一根 ×3.2 的高柱**：「现在好像只有一个特别高的柱子」）：

1. **身体动作先举剑**：`CLIPS["mountainRift"]` 从"跳跃 127-132 + 砸地 229-231"改成
   **123-124（举剑）+ 127-132（跳跃）+ 229-231（砸地）**，11 帧。业主点的那两帧就是
   双手过顶、刃垂在身前的起手式；`SPRITE.skillClips.mountainRift.beats` 跟着分成
   **2 / 6 / 3**：举剑占 0-0.10（落地阶段，跳跃物理在 `leapFrom` 0.07 离地）、跳跃占
   0.10-0.36（滞空）、砸地 0.36 起（`activeFrom` 就是触地那一刻）。测试新增三条：clip 必须
   以 123-124 开头、举剑必须在离地前播完、砸地必须和触地同帧。
2. **火改成一排柱子**：两波各**五处**，间距 130 客户端 px（外两株在 ±260，正好落在裂缝
   自己那道环上——`floor` 环 283×112 放大 1.8 倍后两侧各约 254px）。上一片是 110px 间距、
   ×0.60-0.70，五株挨在一起糊成一条"矮火带"，读不出"多个"；现在外侧留出缝，五株各自成柱。
   中间那株最高、先起，外面两株依次矮一档、晚一点起（不是一排等高的栅栏）。
3. **高度取客户端预览那一档**：火丛 ×0.85/×0.75/×0.65（108/95/83px）、岩浆柱
   ×0.70/×0.60/×0.50（125/107/89px），约 1.05-1.55 个角色高——正是客户端自己预览里那团火
   的高度，也仍然比"特别高的柱子"矮得多（旧的 ×3.2 单柱是 4.5 个角色高、顶到屏幕外面）。
   两半共用同一个窗口与缩放，所以火必须留在 `mountainRift` 行已经要的那块窗口里，
   不然裂缝自己那道环会被一起缩小。
4. **顺带**：收尾火星跟着涨到 ×0.75/×0.65（原来 ×0.70 配的是矮一档的火）。

数值一览（`mountainRiftFire` 行 45 帧、19 段窗口）：补光 ×0.9 · 火丛
×0.85/×0.75/×0.75/×0.65/×0.65（第一波五处，`y -45`）· 岩浆柱
×0.70/×0.60/×0.60/×0.50/×0.50（第二波五处，`y -15`）· 岩浆滴 ×1.3 · 碎岩 ×1.0 ·
收尾火星 ×0.75/×0.65/×0.65；`SPRITE.skillClips.mountainRift = { row: 6, first: 9,
frames: 11, beats: [2, 6, 3] }`（同一行里 `silverFall` 20 起、`jump` 28 起，跟着往后挪两格）。
测试里那条"每一处火都在 ×0.4-1"改成 **×0.45-0.9**（柱子，不是栅栏也不是火墙）。

验收：`npm test` 153/153、`npm run test:browser` 双通；慢放逐帧
（`node tests/browser/capture-effect.mjs mountainRift 40 0.09 520`）：起手举剑 → 跃起时血剑
落下 → 触地那一下裂缝开、五株火从环里依次起来 → 空档期只剩环与裂纹 → 第二波五株柱子再起 →
收尾散成火星，全程没有一根独高的柱子。

第四十八切片让**举剑带血剑**，并把火从"一排柱子"改成**围着人一圈的柱子**（业主：
「崩山裂地斩是先举剑，参考 123-124，应该是血剑」、「它是多个火焰柱子，喷发，但是不是一排
柱子，应该是一个圈」）：

1. **血剑从施法第一帧起就在场上**：上片只有身体动作补了举剑，血剑本身要等落地才从天上下来
   ——举剑抬的是一把还没出现的剑。现在效果行从施法第一帧就开始画
   （`EFFECT.timing.mountainRift` 从 `{ from: 0.26 }` 改成 `{ from: 0 }`），包里的血剑手势
   拆成两段窗口：**聚形 + 横扫 （f0-f12）跑在举剑与跳跃期间**（0.00-0.71s，火刃在头顶聚起来
   跟着他一起压下来），**砸地那几帧（f13-f19）落在触地那一下**（0.71-0.91s，就是
   `activeFrom`）。
2. **火摆在那圈光环上，不再摆成一条线**：上一片五株火都站在同一条地平线上（间距 130px），
   看着是排不是圈。现在落点取的是**裂缝自己那圈光环**——包里 `floor` 是 445x166、环心就是
   站人点 (382, 281)；技能行把它放大 1.8 倍（以底边中心为基准），出来的光环是中心
   **(391.5, 241.5)**、半轴 **195×84** 的椭圆。两波各取椭圆上**六处、每 60° 一处**，第二波
   整体转 30°，两波合起来整圈都喷一遍。每处按"落点 − 图形自己的底边中心"写 `offset`
   （火丛底边中心 (419, 324)、岩浆柱 (474, 282)），火根正好压在光环上。
3. **远侧的火画在人身后**：椭圆有近边也有远边，远侧那几处落在画面上比脚高，画在人前面就是
   糊在他身上。所以远侧的柱子归**技能行**（`drawPlayer` 之前画，和地裂、血剑同一行），
   近侧与两侧留在 `mountainRiftFire` 行（人前面）——圈才有里外，画在同一边就不是圈。
   近处的火最大、最先起（×0.95），远的依次小一档（×0.62-0.88）。
4. **火再长高四分之一**：第一版圈火取 ×0.55-0.80（60-108px），对着客户端的喷发一看还是
   "烛火"（业主：「火焰有点小」），所以整圈都抬到 ×0.62-0.95：火丛 89-121px、岩浆柱
   107-143px，约一个角色高的腰到胸，既不是之前那根 4.5 个角色高、顶出画面的独柱，
   也不是刚能盖住脚踝的一排小火。行窗口由血剑和地裂决定（835×335 客户端 px），火放大后
   没有撑破它，所以裂缝那圈光环的尺寸一点没变。

数值一览：`mountainRift` 行（9 段窗口）= 血剑 ×1.4 / +320 两段（f0-12、f13-19）· 地裂 ×1.8
（裂纹 alpha 0.5、环画在最上）· 远侧第一波火丛 ×0.70 `(-28,-167)` · 远侧第二波岩浆柱
×0.62 `(15,-113)`、`(-180,-113)`；`mountainRiftFire` 行（18 段窗口）= 补光 ×0.9 ×2 ·
第一波火丛 ×0.95 `(-28,2)` / ×0.88 `(-196,-41)` / ×0.88 `(141,-41)` / ×0.78 `(-196,-125)` /
×0.78 `(141,-125)` · 第二波岩浆柱 ×0.80 `(-180,32)` / ×0.80 `(15,32)` / ×0.70 `(-278,-41)` /
×0.70 `(113,-41)` · 岩浆滴 ×1.3 · 碎岩 ×1.0（`"board": ""`）· 收尾火星 ×0.95/×0.82/×0.82；
`EFFECT.timing.mountainRift = { from: 0, to: 0.99 }`。
测试里那条「每个技能在施法起点不画特效」改成**允许自带起手式的技能（`timing.from === 0`）
从第一帧就画**，`大蹦 plays in stages` 新增四条：每一处火都站在这道光环的椭圆上
（±5%）、远侧的火画在技能行、火横向铺开 >300px 且纵向 >120px（是圈不是排）、最近那株最大
且最先起；火的高矮那条从 ×0.45-0.85 放到 **×0.45-1**（这片把火抬高了四分之一）。

验收：`npm test` 153/153、`npm run test:browser` 双通；慢放逐帧
（`node tests/browser/capture-effect.mjs mountainRift 46 0.09 510`）：起手举剑时血剑在头顶聚形
→ 带着血剑跃起 → 触地那一下裂缝开、血剑砸进地面、一圈低火沿光环起来 → 空档期只剩环与裂纹
→ 第二波柱子从光环的另外六处拔起 → 收尾散成火星。

第四十九切片按业主对着**逐帧对照图**（`assets/dnf_effect_anim/ingame-mountainRift-ring.png`：
上排客户端预览 `OutRageBreak.avi` 的落点 f23 / 第一波 f24-31 / 第二波 f60-93，下三排实机
起手 / 第一波 / 第二波）的第八轮反馈再收一遍（业主：「火焰还是小，感觉也有多余动作」）：

1. **多余动作 = 那一跳，它没了**：实机「起手」那九帧里鬼剑士**腾空往右上跳**，同时一道火
   从屏幕左上斜着划到右下——那是借来的 127-132（崩山击／普通跳跃的动作）加上血剑那一层
   在身后聚形。把客户端预览起手的 20 帧量一遍：人的头顶**一直在同一水平线**上，他是站着的。
   所以 `CLIPS["mountainRift"]` 从 `123-124 + 127-132 + 229-231`（11 帧）换成
   **`123-124（举剑）+ 125-128（刃在身前压下来）+ 133（深蹲把剑钉进地）`（7 帧）**，
   `beats` 2/6/3 → **2/4/1**，`SKILLS.mountainRift` 的 `leap / leapUp / leapFrom` 三个字段
   整段删掉——判定、360px 裂地波、三段伤害（0.72 / 1.13 / 1.54s）一个不动。测试新增
   「整招没有跳跃」（大蹦的 `leap` 必须是 `undefined`、崩山击仍然有 leap、烘焙代码里不许再
   出现 127-132 那一段），并把「举剑必须在离地前播完」换成「钉地那一拍落在 `activeFrom`」。
2. **血剑从身后搬到人前**：它原先写在技能行（画在人身后）并且 `offset (320,0)`、放大 1.4 倍，
   于是整段聚形读成一颗横穿屏幕的火彗星。现在它进了 `mountainRiftFire` 行（画在人前面），
   `offset (200,-20)`、聚形 ×1.0／砸地 ×1.2，两段窗口 0.00-0.33 / 0.33-0.47——正好是
   举剑-压下-钉地那条时间线，砸地那帧落在 0.72s 的判定上。
3. **火再放大一倍**：客户端预览里第一波的火是 **2.1 个角色高**、第二波 2.5 以上，
   而我们第二波只有 107-143px（约一个角色高）。火丛 ×0.95/0.88/0.78 → **×1.90/1.75/1.55**
   （实高 234/215/191px）、远侧 ×0.70 → ×1.40；岩浆柱 ×0.80/0.70/0.62 →
   **×1.60/1.40/1.25**（278/243/217px）；补光 ×1.35、岩浆滴 ×1.6、碎岩 ×1.15、收尾火星
   ×1.90/×1.65。最高那根柱子顶在 y≈150，仍在画面里、不盖住顶部 HUD。
4. **两半各用各的窗口（顺便治糊）**：放大一倍后火全是糊的——技能行与火行共用同一个窗口
   （811×436 客户端 px 的裂缝），整行压进 128px 的 cell，一朵 240px 的火只分到 35 个 cell
   像素。现在 `import_dnf_effects.py` 不再把两行的 ink window 求并集：`mountainRift` 用
   811×436，`mountainRiftFire` 用 553×379；渲染端新增 `EFFECT.frontDraw`（大蹦
   `{ dx: 20, dy: -143, size: 572 }`）。两半仍然严丝合缝，因为两行都把**人脚那一点**烘在
   cell 的同一处（128 里的 64, 96），而 cell 画出来时那一点落在 `人 + (dx, dy + 0.25 × size)`，
   所以 `dy = -210 + 0.25 × (840 - 572) = -143`；`size` 让一个客户端像素在屏幕上一样大
   （840/811 = 572/553 = 1.035）。附带的好处：以后改火不会再牵动裂缝那道环的大小。

数值一览：`mountainRift` 行 = 地裂 ×1.8（碎裂 / 环 / 环留住 / 裂纹 alpha 0.5）· 远侧第一波
火丛 ×1.40 `(-28,-167)` · 远侧第二波岩浆柱 ×1.25 `(15,-113)` / `(-180,-113)`；
`mountainRiftFire` 行（20 段）= 血剑 ×1.0/×1.2 `(200,-20)` · 补光 ×1.35 ×2 · 第一波火丛
×1.90 `(-28,2)` / ×1.75 `(-196,-41)` / ×1.75 `(141,-41)` / ×1.55 `(-196,-125)` /
×1.55 `(141,-125)` · 第二波岩浆柱 ×1.60 `(-180,32)` / ×1.60 `(15,32)` / ×1.40 `(-278,-41)` /
×1.40 `(113,-41)` · 岩浆滴 ×1.6 · 碎岩 ×1.15 · 收尾火星 ×1.90/×1.65/×1.65；
`SPRITE.skillClips.mountainRift = { row: 6, first: 9, frames: 7, beats: [2, 4, 1] }`
（`silverFall` 20 → **16**、`jump` 28 → **24**）；`EFFECT.timing.mountainRift = { from: 0,
to: 0.99 }`。测试还改了：血剑在前排找（身后那行一根都不许有）、两段窗口容差 0.08s、
火的高矮 ×1.2-2、以及「shipped DNF effect sheet」那条允许大蹦自己那行**前面留空**
（它的第一个动作就是落地），改成「列必须连续、且一直排到最后一列」。

验收：`npm test` 153/153、`npm run test:browser` 双通；慢放逐帧
（`node tests/browser/capture-effect.mjs mountainRift 30 0.08 480`）：站着举剑、血剑在手前聚形
→ 刃压下来时裂缝在脚下开 → 一圈两倍大的火沿光环起来（人始终在地上）→ 空档期只剩环与裂纹
→ 第二波更高的岩浆柱从另外六处拔起 → 收尾散成火星。

第五十切片修掉上片留下的一个**姿势**（业主：「放完技能多了一个不正确的动作，歪着身体举剑
那个动作」）：

1. **那一帧是 133**。上片把身体动作收在 `123-124（举剑）+ 125-128（压下）+ 133`，`beats` 是
   2/4/1——最后一拍只有一帧，于是从 0.72s 到施法结束的 **1.28 秒**里他一直保持着 133。而 133
   是**单腿抬起、身体歪向一侧、刃朝上**的平衡姿（`assets/dnf_src/full-frames/frames-122-181.png`
   第一行第 12 格），既不是这一招的砸地，也不是任何收招——所以火都喷完了，人还歪在那里举着剑。
   这就是「放完技能多出来的那个动作」。
2. **收在压下的最低点 128**：`CLIPS["mountainRift"]` 改成 **`123-124 + 125-128`（6 帧）**，
   `beats` 2/4——第二拍在 0.36（=`activeFrom` 0.72s）走完，之后**渲染器自然停在这一拍的最后
   一帧**（`playerFrame` 的 beat 循环走到头就保持末帧），也就是两腿压低、剑已经劈进身前地面的
   那一格，和客户端预览里落点那一下的前倾姿势对得上。血剑／火／地裂那一行一点没动，仍然在
   0.72s 落在判定上。

测试：`rift.frames` 11 → 7 → **6**、`beats` 改成 `[2, 4]`；烘焙那条正则改成
`\[123, 124\] \+ list\(range\(125, 129\)\)` 并**新增一条「不许再出现 133」**（就是业主点掉的那个
平衡姿）；「钉地那一拍」改成「压下的最后一拍 == `activeFrom / duration`」；另外新增一条
**用渲染器自己取一次帧**：把施法进度拨到 0.9（火还在烧的时候），`Render.playerFrame` 给出的
列必须是这一段 clip 的最后一格——业主看到的就是这一格，它变了测试就红。

验收：`npm test` 153/153、`npm run test:browser` 双通；慢放逐帧
（`node tests/browser/capture-effect.mjs mountainRift 30 0.08 480`）：站着举剑 → 刃在身前
压到最低、裂缝开、火起来，人**一直保持压在剑上的低姿**，不再出现歪身举剑的收尾。

第五十一切片照业主给的**训练房实拍**（`assets/dnf_src/bilibili/skill-clips/10_崩山裂地斩.mp4`，
136 帧 @30fps）逐帧重量，推翻了第八·九轮按客户端预览 `OutRageBreak.avi` 定的四条基线
（明细与复算在 `assets/dnf_effect_picks.md`）：

1. **跳回来了**：参考 #33→#50 量到滞空 **0.60s**、升高 **269 参考px = 101 本作px**、前移 47px；
   反推 v₀=673、g=2244，正好是本作默认重力 2200，所以不加专属重力（`leapUp -666`）。腾空无敌且不闪。
2. **时长 2.0s → 4.0s**：按键→举剑→跳→落地伏地→第一波→空档→第二波→起身，三段伤落在 **1.14 / 1.97 / 2.80s**。
3. **火改红**：参考火体 **(183,25,7)**，而同一条片子里 01 崩山击复量是 **橙 (233,69,35)**，
   所以不是视频偏色。新增逐层 `ramp`（按像素最亮通道在色标间插值），烘出来的火体均值 (179,25,9)。
4. **落点从「围人一圈」改成「朝前单侧裂谷」**：`radius 190 → 0`、`reach 200 → 245` 的前向盒子，
   引擎那圈椭圆范围一起关掉（`arc: false`）。

身体动作换成 `123,124 举剑 → 204,205 空中 → 208,209 落地伏地 → 132 起身`，`beats` 2/2/2/1。
验收：`npm test` 156/156、双通浏览器证明；实机抓帧量到 x 110→157（47px 前移）、最高 335（95px 高）、
顶点 0.85s、落地 1.15s，对上参考的 47px / 101px / 0.87s / 1.14s。

第五十二切片按业主第四次反馈（「**比参考的差远了，请你修复**」）把**画出来的东西**重做一遍。
上一版的判定（跳跃、4.0s、红火、单侧裂谷）是对的，但把参考帧摊开一帧一帧对，差的是四件事：

1. **参考里最大的东西我们几乎没有——那块碎岩地**。参考 #61–#88（空档期）画面里人趴在一片
   **碎岩板**上，板缝里是发光的熔岩线；我们只有 `floor` 后半段的几条细裂纹，还压到 `alpha 0.5`。
   现在用包里的**整个地板**：`f0` 暗色碎岩板 + `f1` 板缝 + `f8–f10` 熔岩亮缝，从落地**一直挂到收招**；
   另外加一条 `ROCK_RAMP` 只提亮度——包里那块地板是近黑的（均值 57,48,44），直接画会陷进本作
   自己的暗地板里，而参考里的岩板是**比背后的黑更亮**的。
2. **火是一整面贴着地的高火墙**，不是一排矮丛。第二波从 5 根加到 **9 根 + 3 丛打底**，沿裂谷从
   u 80 排到 u 435，中间的 2.11 倍（0.596 的比例下 **228 本作px**，人 81px = 2.8 身位）；
   底部两处**白热芯**画在最后，从火舌缝里透出来（参考的火舌之间是看得到白热底的）。
   `FIRE_RAMP` 顶端也从 (255,120,60) 提到 (255,225,195)，让白热芯真的白。
3. **举剑那一下手上有刀**：参考 #45–#51 是**一道长火刃**（约 1.6 身位），我们原先只有贴着头顶的一颗
   火团。血剑聚形段缩到 1.70 倍、贴着抬手的位置。
4. **糊是结构性的**：上一版两排共用一个 ~700px 的窗口压进 **128px 的 cell**（一朵 216px 的火只分到
   ~43 个 cell 像素），再被拉到 470px 画出去——所以怎么调都是糊的。现在 `import_dnf_effects.py`
   给大蹦单开一张**大 cell 的表** `assets/rift.png`（`RIFT_CELL = 384`），两排都烘它：
   同样一朵火分到 ~228 个 cell 像素，1:1 画出去。两排烘到**同一个声明的窗口**
   `(120,-140,960,420)`（840×560 客户端px），所以渲染端只要一个
   `EFFECT.draw.mountainRift = { dx: 0, dy: -125, size: 501, ground: true }`——
   `size = 840 × 0.596`，`dy = -size/4` 就是把锚点（人脚）放回脚上，原先的 `dx 95` /
   `EFFECT.frontDraw` 整段删掉。

时候也按参考挪正：第一波 0.29–0.43、空档 0.43–0.54、第二波 0.54–0.91、火尾 0.88–1.00
（参考是落地炸一下 1.8–2.1s、空档 2.1–2.8s、大火 2.8–4.0s）。

测试跟着改：效果表的两排改从 `assets/rift.png` 读（长度 45、宽度 `riftCell × 45`、前排从第 0 列起、
后排从落地那一列起且不许有空洞）、渲染器两半都必须从 `sprites.rift` 取（少一张图就什么都不画，
而不是退回小表画糊的）、每处火的脚落在裂谷那条线 `y = 289 + 0.12u` 上（±8px）、`ramp` 覆盖前排
**全部**层、地面至少要有一个「从落地挂到收招」的亮缝层。**部署契约也补了一条**：`src/*.js` 里写的
`./assets/...` 必须真的被暂存——少 `rift.png` 是「技能什么都不画」，和当初少 `records.js` 是同一类
事故，而这次的暂存列表里确实漏了它（本地测试全绿，线上会静默少一块）。

验收：`npm test` 157/157、`npm run test:browser` 双通（资产表多一行 `rift.png 200`）；
抓帧 `CAPTURE_OUT=/tmp/mr-new node tests/browser/capture-effect.mjs mountainRift 100 0.1 400`。
对照图：`assets/dnf_effect_anim/ref10-vs-ingame-new.png`（参考上 / 实机下，按人脚对齐）。

第五十三切片照业主第五次反馈（「**感觉还是和 `10_崩山裂地斩.mp4` 有些差异**」）不再靠眼看，改成**先量**：
把参考片和实机抓帧都按**人物身高**归一化（参考的剑士在 1440×1080 里高 256px，本作的在 1120×720 里
高 75px），于是「参考里的火有多高」有了统一的单位——**几个人身**。量出四处对不上的比例：

1. **空档的地面**：参考的空档和收招是**一片点亮的地裂网**（宽 2.32–2.50 身位、地面均值 (117,49,38)、
   41% 是亮红），我们只有 **0.07** 身位宽的亮红。根因是包里的 f7–f10（「橙红裂纹贴地蔓延」）被
   **摊在整个 4 秒里播**，空档期画到的是第九分之一的网，最后一帧才画完。现在落地后 0.27→0.33
   铺开、f10 从 0.33 常挂到 1.00；`ROCK_RAMP` 提亮到 (108,99,89)（参考 (99,95,89)）、`FLOOR_RAMP`
   提热到 (121,60,43)／37% 亮点（参考 (117,49,38)／41%）；地裂网整片从 1.30 倍收到 **0.98** 倍
   （`dy` 35→27，环心仍落在 `y = 289 + 0.12u`）。
2. **第二波的火舌**：参考 2.22 身位高、每根只有 **0.2–0.4** 身位宽；我们那版 2.36 高但每根 1.0+
   宽，9 根挤成一道篱笆、整片 4.04 宽。烘焙新增逐层 `"stretch": (sx, sy)`（按轴各乘一个系数、
   仍绕底心），火舌压到 0.55 倍宽，根数 9→5、间距拉开，底座改成三丛横向压扁的火当那一片亮底。
3. **落地那一炸**：参考只有 1.00–1.13 身位高，我们 1.73 → 第一波从 1.85/1.57/1.35 收到 1.15/1.05/0.90。
4. **空档的火**：参考还留着一排 0.70 身位高的低火，我们掉到 0.37 几乎没了 → 补一排常挂的矮火。

修完实测：落地 **1.11**（参考 1.00–1.13）、第二波 **2.23**（参考 2.22）、空档低火 0.97、空档亮地面
2.85（参考 2.32–2.50，略宽）。**还剩一项没解决**：第二波整片仍比参考宽 21%（3.17 vs 2.61）。

测试跟着改：地裂网从「f8–f10 摊开」改成「落地半秒铺开（`first === 7`）＋ f10 常挂到收招」，新增
「空档期至少还有两处火在烧」，火的 scale 界按新量法改成 0.75–2.1。

验收：`npm test` 158/158、`npm run test:browser` 双通；
抓帧 `CAPTURE_OUT=/tmp/mr-fix3 node tests/browser/capture-effect.mjs mountainRift 100 0.1 400`。

第五十四切片照业主第五次反馈（「**是不是因为我们只有一条线……而且崩山裂地斩后面会被打断**」）。两句话
各对一半，也都指向同一件事：这个技能我们画在**角色的动画**上，DNF 把它画在**场地上**。

`ARENA.groundY` 是一条线（y 只当跳跃高度用），人、怪、判定都在线上——真正的 DNF 有 X/Y 两个地面轴，
崩山裂地斩铺的是地平面上一块。加 Y 轴要动碰撞、所有技能的判定盒、所有怪的 AI、房间和相机，那是重做
游戏，所以这次只做能做的两件事：

1. **打出去的地面交给场地**：挨打断掉时 `damagePlayer` 会把 `skillId` 清空，而效果一直是
   `player.skillTimer > 0` 才画——于是**已经裂开的地在那一帧整片消失**，而且整个效果跟着人走，人被打飞
   火也飞。现在落地那一拍（`field.from = 0.265`）把效果转成一个 **field 记录**（落点、朝向、自己的
   时钟，`Core.spawnField`），渲染端 `drawFields` 照它画：挨打断掉照样原地烧、正常收招不跟着那一帧
   消失（再烧 `linger = 1.4s`）、人走开火留在原地——参考 #135 就是他站着、裂缝还在亮。
2. **收招亮度与深度**：`EFFECT.fade.mountainRift.floor` 0.45 → 0.75（参考最后一帧的地面均值
   (86,26,18)，是空档期 (117,49,38) 的四分之三，地裂网是慢慢暗下去的）；12 根火舌不再全站在
   `y = 289 + 0.12u` 这一条线上，而是各在脊线上下错开 ±26 客户端px，读出「地平面上一块火」。

测试新增：「大蹦打出去的地由场地接管」（打断后仍画、且画在原点而不是人被击飞的位置、烧完 `life`
才灭）、「收招时他站起来火还在烧」；火舌落点的容差从 ±8 放到 ±34（半条带）。`npm test` **160/160**。

验收：`npm test` 160/160、`npm run test:browser` 双通；
抓帧 `CAPTURE_OUT=/tmp/mr-band node tests/browser/capture-effect.mjs mountainRift 96 0.1 500`。

> **2026-09-25 补记：这一段的判断已被推翻。** 见 [`docs/adr/0001-depth-axis.md`](docs/adr/0001-depth-axis.md)：
> 深度轴不做"重做游戏"式的整体重写，而是分五片增量交付，并以「`z = 0` 复现今天的一切」作为兼容性不变量。
> 原文保留不改——它列出的爆炸半径（碰撞、所有技能的判定盒、所有怪的 AI、房间和相机）**一条不差**，
> 那份清单正是分片的依据。

第五十五切片给游戏加**第二个地面轴**。业主说「帮我将游戏增加 y 轴，能上下移动，这样很多技能渲染就
更真实了」——但 `y` 在这里早就等于"离地高度"，所以新轴叫 `z`（深度），`y` 的物理一个字不动。第五十四
切片判过这件事"那是重做游戏"：**那句话没有错**（它列的爆炸半径一条不差），推翻的是"所以不做"这个结论，
改成分五片交付，记在 `docs/adr/0001-depth-axis.md`，术语收进 `CONTEXT.md`。

这一片只做地基，并且拆成两笔来切，因为第一笔可以**被证明没有动过画面**：

1. **模型与投影**：`Core.DEPTH.scale = 0.22` 收成全库唯一的常量——它本来散在四处地面椭圆里，现在同时
   是"深度→屏幕纵向"的斜率，也就是一台 12.7° 的极浅机位。`depthLift(z)` 是唯一的投影入口，
   `feetY` / `floorY` 两个助手把每一处"把东西放在地面上"的调用都接了过去，人、怪、子弹、掉落、
   贴地特效全走它。人、怪、子弹、掉落都带上了 `z`，而子弹与掉落的 `z` 取自发射它／倒下它的那个身体：
   今天恒为 0，等怪物有深度那天它们会自动落对地方，一行都不用再改。
2. **地板本身**：`Core.BAND = {backY: 310}`。墙从原来一直铺到地面线（430）收在 310，310..430 成为一条
   向屏幕里退的地面带，木纹间距按"离镜头多远"的平方张开——这是 12.7° 机位唯一能给的纵深线索，这个投影
   没有水平收敛可言。亮度的方向没有另发明一套：沿用原本"靠墙亮、靠近镜头暗"，所以接缝处不会断层。
   房间可以自带 `band`，不带就用默认那条。

**凭什么敢说"没动过"**：临时写了一个逐帧录制**每一次 canvas 调用**的轨迹工具，5040 帧（attract 演示
3600 帧 + 强制进沉没礼拜堂 1440 帧）在第一笔前后**哈希逐帧一致**；第二笔对账显示变化只落在背景（墙、
砖、火把、新增的地面带），从裙边往后的每一笔——门、危害、敌人、特效、HUD——逐字相同；再把 3600 帧演示
加 900 帧脚本跑的**完整状态**逐个字段比对，只有 7 处差异、全是新增字段且值为 0，没有任何一个既有数值
变过。这三条加起来才叫"复现今天"，不是嘴上说的。

测试新增 6 条：一条**绊线**（"没有任何东西离开地面线"，哪一片给谁深度它就在哪一片失败、然后被换成真的
那条）、投影与地面带不能互相漂移（往返差 1e-9 像素——双精度经 0.22 往返的极限，也正是"一个常数"这条
决定的实测代价）、房间自带地面带、房间能给怪指定深度、子弹落在施法者那一档、掉落落在尸体那一档。
`npm test` **166/166**。

验收：`npm test` 166/166、`npm run test:browser` 双通；
抓帧 `CAPTURE_OUT=/tmp/floor-after node tests/browser/capture-floor.mjs`（六张：标题页、入场、房间中、起跳、
礼拜堂塌陷前后、Boss 房），与改动前的 `/tmp/floor-before` 并排看——改动前角色像贴在墙报的底边上，
改动后他们站在一块地上。

第五十六切片让玩家**在第二条轴上真的走起来**。上一片把地面做成了带，但没人能走上去；这一片是把 `↑`
从跳跃手里拿过来。

**`↑` 是这一片真正的代价。** 它当时已经是跳跃键，而且写进了操作表（`C`（也支持 `↑` / `Space`））。`W`/`S`
顶不上——`KeyS` 是槽位 1（崩山击）、`KeyW` 是槽位 7——所以键盘上只剩 `↑`/`↓` 两个空键，二选一只能留走位。
DNF 自己就是这么分的：四个方向都走位，跳跃是 C。README 操作表、屏幕提示、帮助页三处跟着改了。

还有一件事是跑测试才发现的：**浏览器端到端套件自己一直在用 `↑` 当跳跃键**（`KEY_FOR_ACTION.jump`）。
不改它，键盘通路的"跳跃"会静默变成"走进屏幕"，而套件照样全绿——它按的是动作名字，不是那个键。
已改成 `KeyC`，并给它加了一次**深度探针**：两条通路（真实按键 / 真实指针事件）开局各走一次进屏幕再
走出来，断言 `z` 确实离开过地面线、又被走回来了。在这之前，第二条轴被接到错误的键上不会有任何东西发现。

**走位本身**：速度按**屏幕空间**定——`PHYSICS.depthSpeedRatio = 0.75`，即屏幕上纵向 198px/s（横着跑是
265px/s），穿过整条带约 0.6 秒；按地板单位定会让它爬得像蜗牛（58px/s）。走位不改朝向（人朝左右、斜着走），
只按上下也播跑步动画，出招/受击时与横向一样被压制，撞到带的前后缘是夹住而不是滑动。

**深度排序**：绘制顺序从此按深度排，远的先画。这是这一片唯一有风险的改动——原顺序是手工的画家算法，
而且施法者自己的技能层恰好夹在"拾取物"和"玩家"之间——所以排序拆成两趟（玩家身后的、玩家身前的），
技能层留在原处不动。**凭什么敢说这趟重构没动画面**：5040 帧的渲染轨迹与上一片**逐帧一致**。演示机器人
从不按方向键，于是全世界 `z = 0`，而深度排序在同深度时保留了原来的次序，所以它是可证明的空操作。

**触屏**：`←`/`→` 原样不动，正上方加 `↑`/`↓`，四键 86×86 的 2×2 块；触屏通路因此不欠账。

测试新增 6 条：走进屏幕再走出来、前后缘夹住、**穿过整条带约 0.6 秒**（把"手感"钉成数值断言）、
走位不改朝向（只有左右能转身）、出招时走位被压制（带一个自由走位的对照量）、地面带更浅的房间更早停住。
原来那条绊线改名为「没有任何房间和怪有自己的深度」——玩家现在有了，房间和怪还没有。`npm test` **173/173**。

验收：`npm test` 173/173、`npm run test:browser` 双通（含深度探针）；
抓帧 `CAPTURE_OUT=/tmp/floor-depth node tests/browser/capture-floor.mjs`——07/08 是走位（带中间与后缘），
09/10/11 是排序：把怪放到玩家同一个 x 上、三个深度（玩家身后、玩家身前、与他齐平），看谁压在谁身上。

## 运行

```bash
npm test          # 173 个核心逻辑、Boss/小 Boss 机制、深度轴与地面带、强化与通关记录、配乐调度、标题演示、结算与暂停战绩、分享链接、配速线、历史最佳、触屏暂停、血之狂暴姿态与血球、技能特效烘焙与渲染、发布暂存契约与渲染冒烟测试
npm run test:browser # 无头 Chromium 跑真实页面：键盘 + 触屏两条通路各通关一次（含一次纵深探针）
npm run test:live # 线上产物冒烟：核对线上字节是否与本地一致，并在无头浏览器里跑一次真实页面
npm run serve     # 起本地静态服务，然后打开 http://localhost:8080
node tests/render-trace.js write /tmp/t.json   # 可选：逐帧录下每一次 canvas 调用（比较用 compare）
python3 assets/make_slayer_sprites.py   # 可选：重新生成原创精灵图与技能图标
python3 assets/make_slayer_rift_candidates.py   # 可选：重出「跟崩山裂地斩同型」的候选图（预览 + 全部帧）
```

直接在浏览器里打开 `index.html` 也可以玩。

## 操作

按键沿用 DNF 鬼剑士布局：方向键移动，`X` 普攻，`C` 跳跃，`A` / `S` / `D` / `F` 释放技能。

| 按键 | 动作 |
| --- | --- |
| `←` / `→` | 左右移动 |
| `↑` / `↓` | 走进 / 走出屏幕（纵深走位；`↑` 不再是跳跃） |
| `C`（也支持 `Space`） | 跳跃 |
| `X` | 普攻（连按打出四段连击，伤害 8 / 10 / 11 / 15；一次按键只出一刀） |
| `Z` | 上挑（DNF 默认键位；技能栏里也有一个槽位可以放它） |
| `A`~`H` / `Q`~`Y` | **双排 12 个技能槽**（上排 A~H，下排 Q~Y；默认填 11 个技能，留 1 空槽） |
| `B` | 打开技能编成面板（拖动图标换槽，再按 B 关闭） |
| `1` / `2` / `3` | 选择清房后翻开的强化卡（触屏直接点卡片） |
| `N` | 换一个新种子并重开（想固定种子就用 `?seed=123`） |
| `P` | 暂停 |
| `F3` | 重开 |
| `F1` | 显示/隐藏帮助（帮助盖住画面时战斗会暂停，收起后继续） |
| `M` | 静音 / 恢复音效（记住上次选择） |
| `F2` | 手动切换屏幕虚拟按键 |

## 技能编成（可拖动）

技能栏是 **DNF 式双排 12 个槽位**（上排 `A S D F G H`，下排 `Q W E R T Y`），槽位上的技能可以自由拖动
编排，和 DNF 的技能栏编成一样：

- 按 `B`（或点触屏右上角的「编」）打开编成面板：面板上方是两排槽位，下面是全部 11 个技能的图标卡片
  （带 MP / 冷却 / 效果标签）。
- 用鼠标或手指把卡片**拖到槽位**即可装上；如果该技能已经在别的槽，会与目标槽**互换**；
  把槽位上的技能拖到另一个槽也会互换。
- 编成结果写进 `localStorage`（键名 `nano-dnf-loadout`），刷新后保持。面板关闭时点/按槽位就是正常释放技能。
- 面板与拖拽逻辑放在 `src/loadout.js`（纯函数：`assign` / `swap` / `clearSlot` / `serialize`），因此可以脱离 DOM 单测。

## 触屏与音效

- **虚拟按键**：粗指针设备（手机/平板）会自动开启，画布下方出现 **← → ↑ ↓ 四向块**（下排左右、上排进出
  屏幕）、跳、攻，以及上挑 / 崩山击 / 十字斩 / 鬼斩四个技能键；技能键上显示官方图标与冷却读秒，
  右上角是「停」（暂停，暂停时翻成「续」）/「编」/「音」三个开关。
  桌面端按 `T` 或访问 `?touch=1` 也能看到这套按键，且支持多指同时按住（一边按 → 一边按攻击）。
- **音效**：全部用 WebAudio 合成，不加载任何音频文件——命中是钝击声、技能是扫弦、受击是低频噪音、
  击杀是下滑音、升级三音上行、通关四音上行、进房间短促铃声。首次按键/触摸时才创建 `AudioContext`
  （浏览器要求用户手势），`M` 或右上角按钮可静音并记住选择。
- **背景音乐**：也是程序化合成，同样不加载音频文件。`src/music.js` 是纯音符调度器
  （A 小调 i–VI–III–VII 四小节循环，96 BPM：低音三角波打拍、方波琶音走旋律、每拍一记带通噪声当踩镲），
  可以脱离 AudioContext 单测；`main.js` 用 0.25s 预读的调度器把音符排到音频时钟上，
  并且走一条**独立的音乐总线**——所以 `M` 静音是把总线增益归零，而不是让每个音符自己判断。
  和音效一样在首次输入后才启动。

## 技能

技能按 DNF 的**行为**复刻：每个技能都有 MP 消耗、独立冷却、多段判定与状态效果，随技能等级
（这里等同于角色等级）提升伤害。倍率是本项目的近似值，形态与判定逻辑对齐 DNF。

| 技能 | 按键 | MP | 冷却 | 判定 | 伤害 | DNF 式效果 |
| --- | --- | ---: | ---: | --- | --- | --- |
| 上挑 | `A` | 8 | 1.5s | 1 段 | 12 +3/级 | **浮空**：把敌人挑飞，浮空中敌人无法出手，可继续空中连击 |
| 崩山击 | `S` | 18 | 5.0s | 1 段 + 冲击波 | 22 +4/级（波 10 +2/级） | 前跃砸地，**倒地**；5 级起冲击波范围加宽 |
| 十字斩 | `D` | 14 | 4.0s | 2 段 | 18 +3/级/段 | 十字判定，2 级起附加**出血**（每秒 4 +1/级，持续 3 秒） |
| 鬼斩 | `F` | 25 | 8.0s | 3 段 | 14 +2/级/段 | 慢速鬼气三连斩，命中期间**定身 + 硬直**，不产生击退 |
| 三段斩 | `G` | 12 | 3.0s | 3 段 | 9 +2/级/段 | 边推进边三段斩（每段向前踏一步） |
| 裂波斩 | `H` | 16 | 4.5s | 1 段 + 上升波 | 16 +3/级（波 8 +2/级） | 上升波把目标**挑飞**，6 级起波的范围更宽 |
| 怒气爆发 | `T` | 24 | 6.5s | 2 段 | 20 +3/级/段 | 以自身为中心的**范围爆发**，把周围敌人击倒 |
| 月光斩 | `Y` | 18 | 5.0s | 1 段 | 26 +3/级 | 月光弧形斩击，小幅击倒、范围更长 |
| 抓头 | `U` | 20 | 7.0s | 2 段 | 24 +3/级 | **抓取**：把目标拎到身前定住，砸地**击倒**并**吸取生命**（回复 25% 伤害） |
| 鬼影闪 | `I` | 22 | 6.0s | 2 段 | 18 +2.5/级 | **位移**：向前瞬闪穿过目标，期间**无敌**，穿过时造成伤害与硬直 |
| 崩山裂地斩 | `O` | 40 | 12.0s | 3 段 + 裂地波 | 30 +4/级（波 18 +3/级） | **大招**：站着举剑、把剑钉进脚下的地（不跳），360px 裂地波把范围内敌人**击倒**，裂缝持续喷发两波火柱 |

其他 DNF 式规则：

- **技能取消**：普攻的收招阶段可以直接取消接技能（DNF 的取消连段）。
- **浮空连击**：命中空中敌人会把它重新托起，HUD 连击数会显示 `AIR COMBO`。
- **倒地 / 硬直**：倒地的敌人在起身前无法行动；被硬直（鬼斩）时同样不能还手。
- **霸体**：Boss 在重踏前摇期间进入霸体，轻攻击不能打断它（但伤害照常结算）。

HUD 左下角是技能栏，显示按键、技能名、MP 消耗与冷却读秒；敌人头顶会出现出血滴、倒地弧线与硬直星环等状态提示。

每个技能释放时还会播放**官方 DNF 刀光**（`assets/effects.png`，由 `assets/import_dnf_effects.py` 从原始 IMG 烘焙）：
上挑用 `upperslash` 的蓝色上挑弧、崩山击用 `blast-front` 的火焰爆裂、十字斩用 `atgorecross/cross`
的红色斜刀光镜像成十字、鬼斩用 `atghost` 升起的鬼神。部分特效导出时没有 alpha 通道（DNF 靠调色板与
叠加混合），脚本会把纯黑底键出成透明，再统一对齐、缩放成 4×4 的 128×128 图集。

新增的位移/抓取/大招也各配了官方素材：鬼影闪用 `ghoststep/01_sword_dodge`（紫色瞬闪残影）、
抓头用 `pinchhpregen`（官方抓取吸血光点）、崩山裂地斩用 `grandwavefullcharge_light`（雷电巨浪）。
图集因此改成**每个技能一行**（11 行 × 4 帧的 128×128），没有专属素材的技能复用同一家族的官方刀光。

清空当前房间后右侧传送门点亮，走到最右侧进入下一层；第 5 层击败 Boss 即通关。

## 敌人

> 房间池共 6 间：下面这张表里的敌种分布在 5 间战斗房里，第 6 间是王座。
> 每次跑图按种子抽 4 间战斗房 + 王座，所以同一个种子每次遇到的东西完全一样。

| 敌人 | 行为 | 应对 |
| --- | --- | --- |
| 小兵 | 近身挥击，前摇短 | 三段连击硬拼或绕后 |
| 精英 | 高血量近战，前摇长 | 前摇后撤，收招期反击 |
| 术士 | 保持距离，蓄力后射出有限射程的法术弹 | 蓄力时撤出射程，或跳起躲弹 |
| 冲锋兵 | 下蹲预警后高速直线冲锋 | 预警与冲锋期间横向拉开，收招期反击 |
| 剑卫（小 Boss） | 高血量近战，并会施放长前摇的**旋风横推**（`SPIN`）：超甲、带旋刃、判定比身体宽，且边转边向前推进 | 前摇期间退出横扫轨道，或在其扫到时起跳越过；收招期是最长的反击窗口 |
| Boss | 近战，并会施放带预警的地面重踏（圆形范围）；血量掉到一半后**狂暴**：更快、更痛、重踏范围更大，并会用带预警的**超甲突进**（`LUNGE`）拉近距离 | 重踏预警时跑出范围或跳起躲避；突进前摇时横向拉开，等它收招再反击 |

## 成长与掉落

| 机制 | 数值 |
| --- | --- |
| 经验 | 小兵 10 / 精英 22 / 剑卫 45 / Boss 80 |
| 升级门槛 | 初始 30 点，之后每级 ×1.6（上限 12 级） |
| 升级收益 | 生命上限 +12、魔法上限 +5、攻击 +2，并立即回复 12 点生命 |
| 掉落 | 精英必掉回血球（+18 HP），小兵 50% 掉落，剑卫必掉更大的 +30 HP，Boss 掉落 +42 HP |
| 拾取 | 走进回血球即可自动拾取，14 秒未拾取会消失 |

## 房间强化

清空一间房（Boss 房除外）后会翻开 3 张卡，按 `1`/`2`/`3` 或直接点卡片选 1 张；
没选之前右侧传送门不会开。抽卡用的是内核的种子随机，所以同一个 seed 的跑法完全可复现。

| 强化 | 效果 |
| --- | --- |
| 锐锋 | 普攻与技能伤害 +3（可叠加） |
| 体魄 | 生命上限 +20，并立即回复 20 |
| 灵息 | 每秒回蓝 +3 |
| 鬼气 | 技能伤害 +15%（可叠加） |

## 地牢布局

房间池有 6 间：**遗弃小巷**（小兵）、**血色涵洞**（术士+重甲+小兵）、**骨龛之门**（冲锋兵+重甲）、
**断桥**（术士+小 Boss 剑卫+冲锋兵，永远排在王座之前）、**沉没礼拜堂**（术士+重甲+小兵，替补），
以及最后的**哥布林王座**。每次跑图按种子从 5 间战斗房里抽 4 间、打乱顺序，再接上断桥与王座：

| 例子种子 | 这一局的房间顺序 |
| --- | --- |
| `1` | 沉没礼拜堂 → 骨龛之门 → 血色涵洞 → 断桥 → 王座 |
| `7` | 遗弃小巷 → 沉没礼拜堂 → 骨龛之门 → 断桥 → 王座 |
| `42` | 沉没礼拜堂 → 遗弃小巷 → 骨龛之门 → 断桥 → 王座 |

抽签用的是独立的小随机器（不消耗放置敌人的随机流），所以同一个种子连敌人站位都完全复现；
不同种子除了顺序不同，**跳过的房间**也不同，总敌人数因此会变。

**开局房按威胁度抽**：房间按名单总血量分档，第一间只会从最温和的一半里出。这条是实测逼出来的——
早期版本允许种子把「骨龛之门」（冲锋兵+双重甲，全池第二硬）排在第一间，1 级、0 强化就撞上它，
触屏通路连吃 28 点伤害；限制开局房之后，同一条通路降到 0–7。

## 开局引导

第一次玩的人不必先读帮助页：`src/hints.js` 是一个纯提示选择器（无 DOM），按**紧迫度**排序，
每次只显示一条、每条一局只出现一次，几秒后自动淡出：

| 提示 | 触发 | 文案 |
| --- | --- | --- |
| 陷阱 | 石板开始裂开（窗口只有 1.35 秒，所以优先级最高） | 地面裂开时离开那块石板 |
| 强化 | 清房后翻开强化卡（不选就过不去） | 选一张强化卡：按 1 / 2 / 3，或直接点卡片 |
| 低血 | 生命低于上限 35% | 血量偏低：捡回血球，或先拉开距离 |
| 首次击杀 | 第一次击杀之后 | 四段连击：X 连按四下，第四下是收招重击 |
| 开场 | 开局 1.5 秒且尚无击杀 | ← → 移动 · C 跳跃 · X 普攻 |

提示写在 HUD 下方居中的胶囊里，和奖励卡一样**不会**透过标题/暂停/通关界面显示。
想手动看一眼某条提示，可以在控制台调 `window.nanoDnf.previewHint("hazard")`。

## 标题演示

打开页面的前十秒不该是一张静止的说明图。`src/attract.js` 是一段**自演脚本**，跑在自己的
`Core` 状态上：走到最近的敌人、进入攻击距离后连砍、技能冷却好了就放一个、清空房间后停 0.9 秒
让三张强化卡亮一会儿再选第一张，然后走到最右侧的传送门进入下一间房，一条命打完或超过 45 秒
就从第一间重来。脚本只用 `Core.step(state, input)`，不碰 DOM、不调 `Math.random`，
所以同一个种子每次演出来的一模一样。

整条链路是走完的：清房、选卡、小 Boss 房、王座。Boss 掉到半血进入狂暴时会打出一条
`Goblin King enraged!` 横幅（这条以前被标题横幅盖住，等于没有），击破后画面保持
`Boss down! / LEVEL UP / Dungeon cleared!` 2.5 秒再重演；房间名、狂暴与通关这类演示横幅
统一画在标题横幅**之上**，所以标题页真的在“转播”这一局。想手动看某一段，可以在控制台调
`window.nanoDnf.fastForwardAttract(30)` 把演示一次性推进 30 秒（上限 300 秒）。

关键是**它和真实那局没关系**：演示有自己的 state，主循环只在标题期间用 `meta.attract` 把
演示渲染出来，真实状态既不推进也不受伤（第十切片那条「标题期间 `state.time` 保持 0」至今成立）。
第一次按键或触摸就结束演示，之后按 `F1` 仍然是原来那张冻结的帮助页，而不是回放。

标题期间的画面也换了：不再是整屏盖死的帮助页，而是顶部一条窄横幅（LOGO、`演示 DEMO` 徽标、
当前种子与记录）加一个闪烁的「按任意键开始」，演示在横幅下面看得见。键位表还在页面下方，
`F1` 也还在。

浏览器证明同时盯两头。真实时间内：演示必须在 6 秒内**打完第一间房、亮出强化卡、选牌并走进
第二间房**。然后同一条演示被 0.5 秒一步地快进到**狂暴并通关**（第 34.5 秒进入狂暴、同一秒
击破），期间每一步都重新比对真实状态的 `hp / xp / kills / roomIndex / time / playerX`，
必须**完全没动**；狂暴那一帧会被截图存成 `attract-boss-rage.png`。最后，第一次输入之后
`window.nanoDnf.getAttract().retired` 必须为 `true`。

## 通关记录

每次通关会按种子记一笔成绩，存在浏览器 `localStorage`（键 `nano-dnf-records`）：
该种子的**最快时间**与**当时的等级**，以及这个种子通关过几次。标题界面右上角显示当前种子与记录，
通关界面如果是新纪录会标出「新纪录！」。记录模块（`src/records.js`）是纯函数，不依赖 DOM 或
存储，所以合并规则可以单独跑测试；最多保留 40 个种子的历史，超出的按最久未更新淘汰。

除本种子记录外，标题还会给出**历史最佳**：`Records.bestOverall(store)` 跨所有存过的种子取最快的一次，
连它属于哪个种子一起报出来（`历史最佳 0:52.0 · 种子 20260915`），所以换种子刷图也有一个总目标。
并列成绩归较小的种子，保证同一份存储永远得到同一个答案；读不出来的条目会被跳过，
空存储或脏数据时这一行干脆不出现，而不是编一个成绩。

两个结束画面都给出这一局的战绩：`src/summary.js`（纯函数、无 DOM）把
**种子 / 用时 / 等级 / 强化 / 击杀 / 承伤**排成两列画出来，有房间总数时再加一行
**到达**（`第 3/5 层`，所以失败时也知道自己走到哪）。
这些数字来自结束那一刻的快照——`state.time` 在结束画面背后仍然在走，所以直接读实时 state
会让表里的用时和上面「本次」的时间对不上；主循环在胜负任一结局的那一帧就把这一局冻结下来
（`syncFinishedRun`），通关仍照旧写入种子记录。
强化显示的是名字（`锐锋 · 体魄 · 灵息`），同一张卡拿两次就出现两次，和实际拿到的完全一致。
浏览器证明读到表格后逐格与刚打完的那局对比（含 `Records.formatSeconds` 格式化后的用时）；
通关与失败各查一次——失败那次是真的走 `Core.damagePlayer` 打死的，任一项对不上都会直接失败。

## 沉没礼拜堂：塌陷地板

替补房「沉没礼拜堂」有自己的机制：地面上有两块**会塌的石板**（`src/core.js` 的 `HAZARD`）。
每 4.2 秒一个循环——先裂开 1.35 秒（地上出现 `CRACK` 预警圈、裂缝变红），然后塌陷 0.45 秒，
之后休息。两块石板**相位错开**，所以安全区会来回移动。

塌陷时**站在板上的任何东西**都会吃伤害：玩家 12 点，敌人 24 点并被击倒。也就是说这块地板
不分敌我——把重甲兵引到裂缝上，它会替你挨一下。地板是常驻可见的，预警也给足 1.35 秒，
所以这是"要动起来"的压力，不是阴招。

塌陷还会给三种反馈：地面炸起尘土与碎石、短促但**比地面重踏更重**的屏幕震动，
以及一段专属的低频崩塌音（锯齿低音下滑 + 两层噪声，听感与任何打击音都不同）。
`stats.collapses` 记录塌陷次数，主循环用它触发音效；浏览器证明则从外部盯着每块石板，
断言**每一次塌陷之前都先被观测到裂缝阶段**——预警一旦丢失就会直接报错。

## 发布验证

## 分享预览

页面带完整的 Open Graph / Twitter 卡片标签（标题、描述、绝对 URL、1200×630 卡片图与尺寸），
所以把线上链接贴到聊天工具或社交平台时能渲染出真正的预览卡，而不是一条裸链接。

卡片图 `assets/share-card.png` 由仓库自己生成：`python3 assets/make_share_card.py` 用**本仓库已有的美术**
（Slayer 精灵图 + 技能图标带）在程序化地牢背景上合成，不引入外部素材，重跑脚本结果一致。
`index.html` 里的 `og:image` 指向线上绝对地址，因此发布冒烟会**真的去抓这张卡**，校验
HTTP 200、是 PNG、且实际尺寸与标签声明的 1200×630 一致——部署忘发这张图会直接报错。
离线侧也有一条同源检查：跑一遍 workflow 的暂存脚本，断言 `og:image` 指向的文件确实被暂存了。

只用本地测试还不够——有两个故障是**只有线上才看得出来**的：Pages workflow 曾经写死文件清单，
漏发 `src/records.js`，线上直接 404 白屏；地牢也曾经在标题界面背后继续跑，放着不动就是 0 血。

单局也能分享：结束画面底部给出这一局自己的链接（`本局链接 …?seed=20260915`），
页面下方有一个「复制本局链接」按钮。链接由 `src/summary.js` 的 `seedUrl()` 拼——
纯字符串函数，会保留 URL 上原有的 `touch=1` 之类的参数与 `#hash`，只把 `seed` 换成当前种子，
种子不可信（0、负数、小数、非数字）时干脆不生成链接。浏览器证明会照这条链接开一个新页面，
断言种子与 `layout`（房间顺序）完全一致，再点页面按钮把剪贴板读回来核对——三者对不上都算失败。

暂停时同样看得到这一局：`PAUSED` 下面那张表用的是 `Summary.liveRun(state, seed)`，
直接从跑着的 Core state 取种子 / 用时 / 等级 / 已拿强化 / 击杀 / 承伤 / 到达层数，
所以它不是回忆上一局，而是当前屏幕。浏览器证明会在跑图中按一次 P，逐格与实时状态对比
（实测 `第 2/5 层`、`0:07.4`、`锐锋`、`2` 杀），再按一次 P 确认继续。

跑图时 HUD 下方还有一条**配速线**：`Summary.pace(clock, record)`（纯函数）比较当前用时与这个种子的
最佳成绩——没有记录时写「本种子还没有记录」，有记录就写「领先最佳 8.6s（最佳 0:50.0）」
或「落后最佳 13.3s（…）」，差值按十分之一秒取整。浏览器证明两头都盯：首局（还没记录）必须是
`none`，通关存下记录后重开一局必须是 `ahead`，把时钟推过记录后必须**翻成** `behind` 且差值约 12 秒。

所以除了本地测试，还有两层发布验证：

- **暂存契约（离线，`npm test`）**：直接执行 workflow 里那一步暂存脚本到临时目录，然后断言
  index.html 引用的每个 `src`/`href` 都真的被暂存了，并断言用的是 `cp src/*.js` 通配而不是手写清单。
  上面那个 404 事故会被它当场拦下。workflow 里也有一道同样的守卫，在**上传产物之前**跑。
- **线上冒烟（需要网络，`npm run test:live`）**：抓取线上页面，逐个核对引用资源的 HTTP 状态与
  **字节哈希**（部署落后于 main 会失败），再用无头 Chromium 打开线上地址，等游戏初始化、
  在标题停留 5 秒，断言 `state.time` 仍为 0、未掉血，且没有 console / page / request 错误。

浏览器证明里的**承伤断言**曾经是整局 `<= 24`：机器一忙，策略的决策间隔被拉长，多挨几下就误报
（实测负载高时 29、重跑 7）。现在它检查的是**单次受击的尺寸**——由游戏自己的
`ENEMY_TYPES`（含 Boss 半血后的 `damageScale`）与塌陷地板的 `playerDamage` 算出最大一击（22），
任何一次受击超过它就算失败；整局承伤继续写在报告里供人观察。证明末尾还会**自检**这条门：
伪造一次 60 点受击必须被报出来，13 点的正常受击必须通过——否则自检本身报错。

这两条检查都验证过"不是摆设"：把暂存清单改回旧写法时，离线测试报
`the staged site is missing src/records.js - the deploy would 404 it`；用漏发模块或还原了标题 bug
的本地站点跑线上冒烟时，它分别报出资源缺失与 `the dungeon ran behind the title for 5s`。
Pages workflow 在 deploy 之后还有一个 `verify` job 跑同一套线上冒烟，部署静默落后或页面报错都会让流程变红。

## 结构

| 路径 | 作用 |
| --- | --- |
| `src/core.js` | 纯逻辑内核：物理、连击、伤害、敌人 AI、房间推进。无 DOM 依赖，Node 与浏览器共用 |
| `src/render.js` | Canvas 2D 渲染层：精灵动画、HUD、技能栏、背景与特效，只读状态、不做修改 |
| `src/main.js` | 浏览器入口：DNF 键位映射、精灵图加载、固定步长循环、暂停与重开 |
| `src/attract.js` | 标题页的自演脚本：自带 state 的脚本化输入策略，整条链路演到通关并定格 2.5 秒（无 DOM、可单测、确定性） |
| `src/summary.js` | 战绩表、配速与分享链接：结算用冻结快照、暂停用 `liveRun()` 实时映射、`pace()` 判定领先/落后，另有 `seedUrl()`（纯函数） |
| `src/loadout.js` | 技能栏编成模型：槽位分配、互换、序列化（纯函数，可单测） |
| `assets/import_dnf_art.py` | 从 DNF 原始 IMG 导入鬼剑士 SD 动画与技能图标，烘焙出下面两张图集 |
| `assets/make_slayer_sprites.py` | 备用：纯原创像素美术生成脚本（不依赖任何外部素材） |
| `assets/slayer.png` | 42×5 张 208×144 精灵帧：待机 / 跑动 / 攻击（四刀 0-41）/ 技能（含上挑专属 41-50）/ 受击·倒地·跳跃·下落 |
| `assets/skills.png` | 四个 32×32 技能图标，顺序与技能栏一致 |
| `assets/effects.png` | 4×4 张 128×128 官方刀光帧，顺序与技能栏一致 |
| `assets/import_dnf_effects.py` | 解码 DNF 技能特效 IMG，烘焙出 `effects.png` |
| `assets/extract_bilibili_skills.py` | 把狂战士技能展示视频（`assets/dnf_src/bilibili/`，已 gitignore）按技能切成单条片段 + 逐帧对照图 |
| `assets/skill-rework-prompt.md` | 「把某个技能照参考重做到一模一样」的提示词：空白模板（改最上面的填写区）+ 一份填好的示例 |
| `index.html` | 页面外壳：标题、画布边框、键位说明与状态栏 |
| `tests/core.test.js` | `node:test` 验证内核行为、成长与掉落、确定性、900 帧稳定性、可通关性、精灵帧选择与渲染冒烟 |
| `tests/attract.test.js` | 验证标题演示：不碰真实那局、同种子可复现、不用 `Math.random`、打到狂暴并通关、卡住也能自己重来 |
| `tests/summary.test.js` | 验证结算战绩与分享链接：逐格数值、到达层数夹取、强化名映射、异常输入不吐脏数字、时间格式与 `records.js` 一致、`seedUrl()` 保留其它参数并拒绝不可信种子 |

## 美术

角色是 **DNF（地下城与勇士）官方 SD 鬼剑士**动画帧，技能图标取自 DNF 鬼剑士技能图标集，
由 `assets/import_dnf_art.py` 从原始 `.img` 里解码并按游戏的图集规格烘焙：

```bash
pip install pydnfex pillow          # 解析 DNF IMG 需要
python3 assets/import_dnf_art.py    # 输出 assets/slayer.png 与 assets/skills.png
```

脚本会把 `c_swordman.img`（43 帧：站立 / 行走 / 攻击）里的关键帧重排成待机 4 帧、跑动 6 帧、攻击 3 帧、
技能 4 帧，并用受击帧合成出受击、倒地、跳跃、下落；技能图标按上挑 / 崩山击 / 十字斩 / 鬼斩的顺序取 4 张。
对应的原始素材缓存在 `assets/dnf_src/`（已 gitignore）。

需要注意的是：**DNF 美术版权属于 Neople/Nexon**，这里按项目所有者的要求直接使用，仓库的 MIT 许可
只覆盖代码，不覆盖这些图像；如果要公开分发，请自行确认授权，或改用 `assets/make_slayer_sprites.py`
生成的原创像素美术。

技能图标（`assets/skills.png`）**每个技能一张，全部是官方图标**：项目所有者对照官方图集一张张挑出来的
帧号写在 `assets/import_dnf_art.py` 的 `ICON_FRAMES` 里（上挑 #94、崩山击 #154、十字斩 #132、
鬼斩 #10、三段斩 #18、裂波斩 #6、怒气爆发 #48、月光斩 #160、抓头 #98、鬼影闪 #138、
崩山裂地斩 #172）。图集本身没有名字表，这些对应关系来自人工辨认；若以后新增技能，没给帧号的技能
会自动退回"用它自己的官方特效缩略图"，不会再出现取图越界导致的错图。

图标库本身没有名字映射，所以脚本支持导出带编号的对照图供人工挑选：

```bash
python3 assets/import_dnf_art.py --atlas   # 生成 assets/dnf_skillicon_atlas.png（已 gitignore）
python3 assets/import_dnf_art.py --icons 94,154,132,10,18,6,48,160,98,138,172
```

`--icons` 按技能顺序给出帧号：上挑 / 崩山击 / 十字斩 / 鬼斩 / 三段斩 / 裂波斩 / 怒气爆发 /
月光斩 / 抓头 / 鬼影闪 / 崩山裂地斩（顺序与 `src/core.js` 的 `SKILL_ORDER` 一致）。

### 狂战士技能参考

业主给的 B 站演示视频 `BV1oUDLBaEaK`（狂战士学习「魔狱血刹」前后）里有一段训练房巡演：
黑底、一次一个技能、技能名打在顶部，底部标着「10级技能崩山击」这样的等级。这一段正好是
一套 1:1 的官方技能动画，`assets/extract_bilibili_skills.py` 就按顶部标题的变化把它切成
11 条片段，每条再导出一张"所有帧"的对照图：

```bash
yt-dlp -f "30080+30280" -o assets/dnf_src/bilibili/BV1oUDLBaEaK.mp4 "https://www.bilibili.com/video/BV1oUDLBaEaK/"
python3 assets/extract_bilibili_skills.py            # 输出到 assets/dnf_src/bilibili/skill-clips/
python3 assets/extract_bilibili_skills.py --detect   # 只重算切点，和表里的边界对一遍
```

巡演里的 11 个技能依次是崩山击(10) / 十字斩(15) / 死亡抗拒(20) / 嗜魂之手(25) / 暴走(25) /
血气分流(30) / 血之狂暴(35) / 怒气爆发(35) / 嗜魂封魔斩(40) / 崩山裂地斩(45) / 魔狱血刹(50·觉醒)。
每条的片段、逐帧对照图与 `INDEX.txt` 都落在 `assets/dnf_src/bilibili/skill-clips/`（已 gitignore）。

## 验证

除了单元测试，还有一条**浏览器级可玩性证明**（`npm run test:browser`）：起本地静态服务，用无头 Chromium
打开真实页面，用和单元测试同一套策略驱动机器人通关整座地牢，跑**两条通路**——键盘（真实按键事件）与
触屏（真实 DOM 指针事件，`hasTouch` 上下文 + `?touch=1`），并校验控制台无报错、页面无异常、资源全部
200、WebAudio 已初始化。最近一次结果：

```
keyboard victory=true kills=14 damageTaken=7 seconds=47.4 level=5  upgrades=锐锋×3+鬼气  record=0:47.4 Lv5  music=started(272 notes, bus 0.5)  audio=created/running  drag=月光斩→槽A persisted=true
touch    victory=true kills=14 damageTaken=0 seconds=45.8 level=5  upgrades=锐锋×3+鬼气  record=0:45.8 Lv5  music=started(266 notes, bus 0.5)  touchMode=true audio=created/running muteToggle=ok
consoleErrors=[] pageErrors=[] failedRequests=[]
assets: index.html / loadout.js / main.js / render.js / core.js / slayer.png / skills.png / effects.png / rift.png / favicon.png 全部 200
```

键盘那条通路还会顺便验证编成：按 `B` 打开面板 → 用指针事件把「月光斩」拖到槽 A → 读回
`getLoadout()` 与 `localStorage` 确认已生效 → `resetLoadout()` 复位。截图见
`tests/browser/artifacts/loadout-panel.png` 与 `loadout-applied.png`；本切片新增的「断桥」混合房与
Boss 狂暴第二阶段、精英剑卫的旋风分别见 `room-mix.png`、`boss-phase2.png` 与 `elite-spin.png`
，清房后的强化卡界面见 `upgrade-choice.png`（都由同一次浏览器跑动产出）。每次跑动的日志还会按
房间记录承伤来源，并校验「每个非 Boss 房间恰好拿到 1 张强化」「通关成绩确实写进了 localStorage」，
以及「标题界面停留 3 秒期间 `state.time` 仍为 0、未掉血」，方便判断新机制是否真的生效、
老缺陷是否真的修好。带记录标题界面见 `title-record.png`。

两条通路各自使用独立的浏览器实例（否则两条 rAF 循环会互相抢 CPU，输入时序被拖慢）；
只跑其中一条可以设 `PASSES=touch npm run test:browser` 或 `PASSES=keyboard`。

**截图证据**：`tests/browser/artifacts/` 下只有**能证明改动的那几张**会被提交——常见的是
`playability-title.png`、`playability-keyboard-fight.png`、`playability-keyboard-clear.png`、
`playability-touch-start.png`、`playability-touch-fight.png`、`playability-touch-clear.png` 这六张
（标题、键盘打一场、键盘通关、触屏界面、触屏打一场、触屏通关），再按当片需要补一张它自己特有的
（第五十五切片的地板见 `chapel-hazard.png`，第五十六切片的新四向键见 `playability-touch-start.png`）。

目录里其余三十多张**每次跑动都会重新生成**，而且常常只是因为 HUD 里那行计时文字变了就变
（`用时 1:05.1 → 1:13.7` 这种）——全提交等于每片往 `.git` 里塞十几兆噪声（历史上一度累计到 468MB）。
所以那些图的字节**就停在它上一次被提交的样子**，跑完 `npm run test:browser` 之后会显示为「已修改」：
**丢掉，不要提交**。`git checkout -- tests/browser/artifacts` 一把还原，然后只 `git add` 上面那几张。

## 设计约定

- 逻辑与渲染分离：`Core.step(state, input)` 是唯一的状态推进入口，便于确定性回归测试。
- 固定步长 1/60 秒，渲染帧率与模拟无关。
- 相同 seed 加相同输入必然得到相同结果，测试用这一点做回归。
- 掉落判定也走同一个确定性随机流，因此回归测试可以逐个比对掉落位置与数值。

## 下一步候选

- **深度轴剩下的四片**：玩家走位与深度排序（②）、大蹦与崩山击的贴地锚点（③）、深度判定与容差（④）、
  怪的 AI 与房间数据（⑤）。见 `docs/adr/0001-depth-axis.md`。
- 技能树 / SP 加点与技能等级，让升级选择影响打法。
- 指令输入（例如 `↑`+`X` 触发上挑）作为热键之外的 DNF 式操作。**注意这条与上一行的走位抢 `↑`**：
  真 DNF 靠"同帧按下＝指令、按住＝走"分开，切片 ② 必须把这条规则一起定下来。
- 移动端触屏按键与音效。
- Boss 多阶段与弹幕组合，进一步加大走位压力。
- 装备与技能树，让升级选择影响打法。
